# Accessibility (WCAG) Remediation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Llevar Sadhana a WCAG 2.1 AA corrigiendo los 15 hallazgos de la spec (teclado del timeline, estado/semantica ARIA, movimiento, contraste y foco de paneles).

**Architecture:** Una funcion pura nueva (`stepFromKey`) con TDD para la logica de teclado; el resto son cambios de atributos JSX y CSS verificados a mano (el proyecto no tiene jsdom, mismo patron que i18n y timeline-clip-handles). El conflicto del handler global de teclado (A15) se resuelve con `stopPropagation` en los widgets del timeline, no ensanchando el guard global, para no romper los atajos de modo cuando un boton tiene foco.

**Tech Stack:** React 19, Vite 6, Vitest 3 (solo dominio puro), CSS plano.

**Referencia:** spec en `docs/superpowers/specs/2026-06-14-a11y-audit-design.md`.

---

## Estructura de archivos

| Archivo | Responsabilidad | Accion |
|---------|-----------------|--------|
| `src/domain/keyboard.js` | Funcion pura `stepFromKey(current, key, {min,max,step})` | Crear |
| `src/domain/keyboard.test.js` | Tests de `stepFromKey` | Crear |
| `src/components/Timeline.jsx` | Playhead slider + teclado; markers teclado + `aria-pressed`; track-row `aria-pressed` | Modificar |
| `src/components/TrackClip.jsx` | `aria-hidden` en los 3 handles | Modificar |
| `src/components/ModeTabs.jsx` | `aria-current` en modo activo | Modificar |
| `src/components/AppShell.jsx` | `aria-expanded` en botones de panel; brand `<span>`->`<h1>`; capturar boton disparador | Modificar |
| `src/components/GlobalPanel.jsx` | `aria-pressed` en swatches; foco al abrir | Modificar |
| `src/components/TimerPanel.jsx` | `aria-expanded` en toggle de cues; `aria-live` condicional en instruccion | Modificar |
| `src/App.jsx` | Restaurar foco al cerrar panel; `Escape` cierra panel; prop `announce` a los dos TimerPanel | Modificar |
| `src/styles.css` | `:focus-visible` global + anillo interno en timeline; `prefers-reduced-motion`; `--faint`; estilo `h1` | Modificar |

**Cobertura de hallazgos:** A1 (T10), A2 (T2), A3 (T4), A4 (T3), A15 (T2+T3), B5 (T5), B6 (T6), B7 (T7), B8 (T9), B9 (T9), B14 (T3), C10 (T10), C11 (T10), C12 (T6+T7+T8), C13 (T6+T10).

**Limite deliberado:** la instruccion del `PlaybackBar` (modo Diseñar) NO recibe `aria-live`. B9 se acoto a `TimerPanel`; en Diseñar el usuario edita mirando la pantalla. Fuera de alcance, igual que GAP-6 / 1.4.11.

---

## Task 1: Funcion pura de teclado `stepFromKey`

**Files:**
- Create: `src/domain/keyboard.js`
- Test: `src/domain/keyboard.test.js`

- [ ] **Step 1: Write the failing test**

```js
// src/domain/keyboard.test.js
import { describe, it, expect } from 'vitest';
import { stepFromKey } from './keyboard.js';

describe('stepFromKey', () => {
  const opts = { min: 0, max: 100, step: 5 };

  it('ArrowRight increments by step', () => {
    expect(stepFromKey(10, 'ArrowRight', opts)).toBe(15);
  });
  it('ArrowUp is an alias for increment', () => {
    expect(stepFromKey(10, 'ArrowUp', opts)).toBe(15);
  });
  it('ArrowLeft decrements by step', () => {
    expect(stepFromKey(10, 'ArrowLeft', opts)).toBe(5);
  });
  it('ArrowDown is an alias for decrement', () => {
    expect(stepFromKey(10, 'ArrowDown', opts)).toBe(5);
  });
  it('clamps to max on increment', () => {
    expect(stepFromKey(98, 'ArrowRight', opts)).toBe(100);
  });
  it('clamps to min on decrement', () => {
    expect(stepFromKey(2, 'ArrowLeft', opts)).toBe(0);
  });
  it('Home returns min', () => {
    expect(stepFromKey(50, 'Home', opts)).toBe(0);
  });
  it('End returns max', () => {
    expect(stepFromKey(50, 'End', opts)).toBe(100);
  });
  it('returns null for keys it does not handle', () => {
    expect(stepFromKey(50, 'Enter', opts)).toBeNull();
    expect(stepFromKey(50, ' ', opts)).toBeNull();
    expect(stepFromKey(50, 'a', opts)).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm exec vitest run src/domain/keyboard.test.js`
Expected: FAIL — `stepFromKey` no existe / no se puede importar.

- [ ] **Step 3: Write minimal implementation**

```js
// src/domain/keyboard.js

// Pure keyboard step resolver shared by the timeline playhead and cue markers.
// Returns the next clamped value, or null if the key is not one we handle
// (so the caller can let the event bubble / do nothing).
export function stepFromKey(current, key, { min, max, step }) {
  let next;
  switch (key) {
    case 'ArrowRight':
    case 'ArrowUp':
      next = current + step;
      break;
    case 'ArrowLeft':
    case 'ArrowDown':
      next = current - step;
      break;
    case 'Home':
      next = min;
      break;
    case 'End':
      next = max;
      break;
    default:
      return null;
  }
  return Math.max(min, Math.min(max, next));
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm exec vitest run src/domain/keyboard.test.js`
Expected: PASS (10 tests).

- [ ] **Step 5: Commit**

```bash
git add src/domain/keyboard.js src/domain/keyboard.test.js
git commit -m "feat(a11y): stepFromKey pure helper for keyboard nav"
```

---

## Task 2: Playhead operable por teclado (A2, A15)

**Files:**
- Modify: `src/components/Timeline.jsx`

Hace el playhead un `role="slider"` focusable con flechas/Home/End. `stopPropagation` en las teclas manejadas evita que el handler global de `App.jsx` reaccione (A15). Reutiliza `onSeek` (ya existe) y `formatClockTime` (ya importado).

- [ ] **Step 1: Add the import**

En `src/components/Timeline.jsx`, anadir junto a los imports existentes:

```js
import { stepFromKey } from '../domain/keyboard.js';
```

- [ ] **Step 2: Add the playhead key handler**

Dentro del componente `Timeline`, junto a `handlePlayheadPointerUp`, anadir:

```js
function handlePlayheadKeyDown(event) {
  const next = stepFromKey(Math.round(elapsedSeconds), event.key, {
    min: 0,
    max: durationSeconds,
    step: 5,
  });
  if (next === null) return;
  event.preventDefault();
  event.stopPropagation();
  onSeek(next);
}
```

- [ ] **Step 3: Make the playhead a slider**

Reemplazar el `<div className={playheadDragging ? 'playhead dragging' : 'playhead'} ...>` por:

```jsx
<div
  className={playheadDragging ? 'playhead dragging' : 'playhead'}
  style={{ left: `${progress}%` }}
  role="slider"
  tabIndex={0}
  aria-label={t('timeline.playheadAria')}
  aria-valuemin={0}
  aria-valuemax={durationSeconds}
  aria-valuenow={Math.round(elapsedSeconds)}
  aria-valuetext={formatClockTime(Math.round(elapsedSeconds))}
  onPointerDown={handlePlayheadPointerDown}
  onPointerMove={handlePlayheadPointerMove}
  onPointerUp={handlePlayheadPointerUp}
  onKeyDown={handlePlayheadKeyDown}
>
  <span>{formatClockTime(elapsedSeconds)}</span>
</div>
```

- [ ] **Step 4: Add the i18n key**

En `src/i18n/locales/es.js`, en el grupo `timeline`, anadir:

```js
playheadAria: 'Posicion de reproduccion',
```

En `src/i18n/locales/en.js`, en el grupo `timeline`, anadir:

```js
playheadAria: 'Playback position',
```

- [ ] **Step 5: Verify build and locale integrity**

Run: `pnpm exec vitest run src/i18n/locales/locales.test.js && pnpm build`
Expected: tests PASS (en cubre es), build sin errores.

- [ ] **Step 6: Manual verification**

Arrancar `pnpm dev`, ir a Diseñar. Tab hasta el playhead: foco visible. `ArrowRight`/`ArrowLeft` mueven el seek +-5s; `Home`/`End` van a 0/fin. Con la sesion corriendo, pulsar `Space` con el playhead enfocado: reproduce/pausa (comportamiento global, aceptable). Las flechas NO hacen scroll de pagina.

- [ ] **Step 7: Commit**

```bash
git add src/components/Timeline.jsx src/i18n/locales/es.js src/i18n/locales/en.js
git commit -m "feat(a11y): keyboard-operable playhead (role=slider)"
```

---

## Task 3: Markers por teclado + estado de seleccion (A4, A15, B14)

**Files:**
- Modify: `src/components/Timeline.jsx`

Anade movimiento por flechas a los markers (que ya son `<button>`), `aria-pressed` para el estado seleccionado, y lo mismo en `track-row`. El boton del marker NO tiene `onClick` (la seleccion esta cableada a `onPointerDown`), asi que en `Space`/`Enter` se llama `onSelectCue` explicitamente; `stopPropagation` evita que el handler global dispare play/pause (A15).

- [ ] **Step 1: Add the marker key handler**

Dentro de `Timeline`, junto a `handleMarkerPointerUp`, anadir:

```js
function handleMarkerKeyDown(cue, event) {
  // Space/Enter select the cue. The marker button has no onClick (selection is
  // wired to pointer-down), so call onSelectCue explicitly. stopPropagation keeps
  // the global shortcut handler from also firing play/pause.
  if (event.key === ' ' || event.key === 'Enter') {
    event.preventDefault();
    event.stopPropagation();
    onSelectCue(cue.id);
    return;
  }
  const next = stepFromKey(cue.time, event.key, {
    min: 0,
    max: durationSeconds,
    step: 5,
  });
  if (next === null) return;
  event.preventDefault();
  event.stopPropagation();
  onMoveCue(cue.id, next);
}
```

- [ ] **Step 2: Wire it into the marker button**

En el `<button>` del marker (el del `.map` de `visibleCues`), anadir `aria-pressed` y `onKeyDown`:

```jsx
<button
  key={cue.id}
  className={markerClassName(cue)}
  style={{ left: `${markerPosition(cue)}%`, '--cue-color': cue.color }}
  type="button"
  aria-label={t('timeline.cueAria', { name: cue.name })}
  aria-pressed={cue.id === selectedCueId}
  onPointerDown={(e) => handleMarkerPointerDown(cue, e)}
  onPointerMove={(e) => handleMarkerPointerMove(cue, e)}
  onPointerUp={handleMarkerPointerUp}
  onKeyDown={(e) => handleMarkerKeyDown(cue, e)}
>
```

- [ ] **Step 3: Add aria-pressed to the track-row**

En el componente `TrackRow`, en el `<div role="button" ...>`, anadir `aria-pressed={selected}`:

```jsx
<div
  role="button"
  tabIndex={0}
  aria-pressed={selected}
  className={selected ? 'track-row selected' : 'track-row'}
  onClick={() => onSelectCue(cue.id)}
  onKeyDown={handleKeyDown}
>
```

- [ ] **Step 4: Build**

Run: `pnpm build`
Expected: sin errores.

- [ ] **Step 5: Manual verification**

En Diseñar: Tab a un marker (foco visible). Flechas mueven el tiempo de la cue +-5s; `Home`/`End` a 0/fin; el clamp respeta los limites. `Space`/`Enter` sobre el marker SELECCIONAN la cue (no play/pause). Con un lector de pantalla, el marker y la fila seleccionados se anuncian como "pressed". Mover un marker con flechas no hace scroll de pagina.

- [ ] **Step 6: Commit**

```bash
git add src/components/Timeline.jsx
git commit -m "feat(a11y): keyboard-move cue markers + aria-pressed selection state"
```

---

## Task 4: Handles de clip fuera del arbol de accesibilidad (A3)

**Files:**
- Modify: `src/components/TrackClip.jsx`

Los handles de resize/fade son redundantes con el CueInspector (accesible por teclado), asi que se ocultan a la tecnologia asistiva.

- [ ] **Step 1: Add aria-hidden to the three handles**

En `src/components/TrackClip.jsx`, en los tres `<div>` de handles, anadir `aria-hidden="true"`:

```jsx
<div className="clip-fade-in-handle" aria-hidden="true" {...handleEvents('fadeIn')} />
<div className="clip-fade-out-handle" aria-hidden="true" {...handleEvents('fadeOut')} />
<div className="clip-resize-handle" aria-hidden="true" {...handleEvents('resize')} />
```

- [ ] **Step 2: Build**

Run: `pnpm build`
Expected: sin errores.

- [ ] **Step 3: Manual verification**

El arrastre con raton de los tres handles sigue funcionando igual (resize, fadeIn, fadeOut). Con un lector de pantalla, los handles ya no se anuncian.

- [ ] **Step 4: Commit**

```bash
git add src/components/TrackClip.jsx
git commit -m "feat(a11y): hide redundant clip handles from assistive tech"
```

---

## Task 5: Modo activo expuesto (B5)

**Files:**
- Modify: `src/components/ModeTabs.jsx`

- [ ] **Step 1: Add aria-current to the active mode button**

En `src/components/ModeTabs.jsx`, en el `<button>` del `.map`, anadir `aria-current`:

```jsx
<button
  key={mode.id}
  className={activeMode === mode.id ? 'mode-tab active' : 'mode-tab'}
  type="button"
  aria-current={activeMode === mode.id ? 'page' : undefined}
  onClick={() => onModeChange(mode.id)}
>
```

- [ ] **Step 2: Build**

Run: `pnpm build`
Expected: sin errores.

- [ ] **Step 3: Manual verification**

Con un lector de pantalla, el modo activo se anuncia como "current page". El estado ya no depende solo del color.

- [ ] **Step 4: Commit**

```bash
git add src/components/ModeTabs.jsx
git commit -m "feat(a11y): aria-current on active mode tab"
```

---

## Task 6: Botones de panel, brand h1 y captura del disparador (B6, C13, parte de C12)

**Files:**
- Modify: `src/components/AppShell.jsx`

Anade `aria-expanded` a los tres botones del topbar, convierte el brand en `<h1>`, y hace que cada boton pase su nodo DOM al cambiar de panel para poder restaurar el foco luego.

- [ ] **Step 1: Brand span to h1 (and drop the now-redundant aria-label)**

En `AppShell.jsx`, dentro de `.brand`, cambiar `<span>{t('shell.brand')}</span>` por:

```jsx
<h1 className="brand-title">{t('shell.brand')}</h1>
```

En el mismo bloque, quitar el `aria-label` del contenedor `.brand` (el `<h1>` ya aporta el nombre accesible):

```jsx
<div className="brand">
```

- [ ] **Step 2: Add aria-expanded and pass the trigger node**

Para cada uno de los tres botones (`theme`, `audio`, `settings`), anadir `aria-expanded` y pasar `e.currentTarget` al handler. Ejemplo del de tema (replicar el patron en audio y settings con su panel correspondiente):

```jsx
<button
  className={activePanel === 'theme' ? 'icon-button active' : 'icon-button'}
  type="button"
  aria-label={t('shell.theme')}
  title={t('shell.themeTitle')}
  aria-expanded={activePanel === 'theme'}
  onClick={(e) => onPanelChange(activePanel === 'theme' ? null : 'theme', e.currentTarget)}
>
  <SunMedium size={19} />
</button>
```

Audio:

```jsx
<button
  className={activePanel === 'audio' ? 'icon-button active' : 'icon-button'}
  type="button"
  aria-label={t('shell.audio')}
  title={t('shell.audioTitle')}
  aria-expanded={activePanel === 'audio'}
  onClick={(e) => onPanelChange(activePanel === 'audio' ? null : 'audio', e.currentTarget)}
>
  <Volume2 size={19} />
</button>
```

Settings:

```jsx
<button
  className={activePanel === 'settings' ? 'icon-button active' : 'icon-button'}
  type="button"
  aria-label={t('shell.settings')}
  title={t('shell.settingsTitle')}
  aria-expanded={activePanel === 'settings'}
  onClick={(e) => onPanelChange(activePanel === 'settings' ? null : 'settings', e.currentTarget)}
>
  <Settings size={19} />
</button>
```

- [ ] **Step 3: Build**

Run: `pnpm build`
Expected: sin errores. (`onPanelChange` aun recibe el segundo argumento en App en Task 8; React lo ignora sin romper hasta entonces.)

- [ ] **Step 4: Commit**

```bash
git add src/components/AppShell.jsx
git commit -m "feat(a11y): aria-expanded panel buttons, brand h1, pass trigger node"
```

---

## Task 7: GlobalPanel — swatches y foco al abrir (B7, parte de C12)

**Files:**
- Modify: `src/components/GlobalPanel.jsx`

Anade `aria-pressed` a los swatches de acento y mueve el foco al panel cada vez que se abre o se cambia de panel.

- [ ] **Step 1: Add the imports for refs/effects**

En `src/components/GlobalPanel.jsx`, cambiar el import de React (anadir `useEffect`, `useRef`). Si no hay import de React, anadir al principio:

```js
import { useEffect, useRef } from 'react';
```

- [ ] **Step 2: Add the panel ref and focus effect**

Dentro de `GlobalPanel`, antes del `if (!activePanel) return null;`, anadir:

```js
const panelRef = useRef(null);

useEffect(() => {
  if (!activePanel) return;
  // Move focus into the panel each time it opens or switches panels.
  const node = panelRef.current;
  if (!node) return;
  const focusable = node.querySelector(
    'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
  );
  (focusable ?? node).focus();
}, [activePanel]);
```

- [ ] **Step 3: Attach the ref and make the aside focusable as fallback**

En el `<aside className="global-panel" ...>`, anadir `ref` y `tabIndex`:

```jsx
<aside className="global-panel" ref={panelRef} tabIndex={-1} aria-label={t('shell.globalControls')}>
```

- [ ] **Step 4: Add aria-pressed to accent swatches**

En el `.map` de `ACCENT_PRESETS`, en el `<button className="accent-swatch ...">`, anadir:

```jsx
aria-pressed={accentColor === preset.value}
```

- [ ] **Step 5: Build**

Run: `pnpm build`
Expected: sin errores.

- [ ] **Step 6: Manual verification**

Abrir el panel de tema: el foco entra al primer control del panel. Sin cerrar, pulsar el boton de audio: el foco pasa al panel de audio. Con lector de pantalla, el swatch activo se anuncia "pressed".

- [ ] **Step 7: Commit**

```bash
git add src/components/GlobalPanel.jsx
git commit -m "feat(a11y): focus into panel on open, aria-pressed accent swatches"
```

---

## Task 8: App — restaurar foco, Escape cierra panel, announce (C12, GAP-2, GAP-4)

**Files:**
- Modify: `src/App.jsx`

Cablea la restauracion de foco al cerrar cualquier panel, hace que `Escape` cierre el panel abierto, y pasa la prop `announce` a los dos TimerPanel para que solo uno tenga region `aria-live`.

- [ ] **Step 1: Add the trigger ref and panel-change handler**

En `App.jsx`, junto a los otros `useRef`, anadir:

```js
const lastPanelTrigger = useRef(null);
```

Anadir la funcion handler (junto a las otras `handle*`):

```js
function handlePanelChange(next, triggerEl) {
  if (next && triggerEl) lastPanelTrigger.current = triggerEl;
  setActivePanel(next);
}
```

- [ ] **Step 2: Add the focus-restore effect**

Anadir un `useEffect` (junto a los demas):

```js
useEffect(() => {
  // When any panel closes, return focus to the button that opened it.
  if (activePanel === null && lastPanelTrigger.current) {
    lastPanelTrigger.current.focus();
  }
}, [activePanel]);
```

- [ ] **Step 3: Route AppShell + GlobalPanel through the handler**

Cambiar la prop del `AppShell`:

```jsx
onPanelChange={handlePanelChange}
```

En el `GlobalPanel`, cambiar `onClose`:

```jsx
onClose={() => handlePanelChange(null)}
```

- [ ] **Step 4: Make Escape close the panel (before the input guard)**

`Escape` debe cerrar el panel incluso cuando el foco esta en un `select`/`range`/`textarea` dentro del panel. El guard actual (`if (tag === 'INPUT' || 'TEXTAREA' || 'SELECT') return;`) esta al principio de `handleKey` y haria `return` antes de procesar Escape. Hay que **mover el destructuring arriba y manejar Escape antes del guard**, y **quitar el viejo `case 'Escape'` del switch**.

Localizar el comienzo de `handleKey`:

```js
function handleKey(event) {
  const tag = event.target.tagName;
  if (tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT') return;

  const { session, activeMode, zenMode, timerPanelProps, handleNudge, setActiveMode, setZenMode } = shortcutsRef.current;
  const inPractice = activeMode === 'practice';
```

y reemplazarlo por:

```js
function handleKey(event) {
  const { session, activeMode, zenMode, activePanel, timerPanelProps, handleNudge, setActiveMode, setZenMode, setActivePanel } = shortcutsRef.current;

  // Escape closes an open panel from anywhere, including form controls inside it.
  if (event.key === 'Escape') {
    if (activePanel) { setActivePanel(null); return; }
    if (zenMode) { setZenMode(false); return; }
    return;
  }

  const tag = event.target.tagName;
  if (tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT') return;

  const inPractice = activeMode === 'practice';
```

Localizar el viejo `case 'Escape'` del switch y **eliminarlo** (ya se maneja arriba):

```js
        case 'Escape':
          if (zenMode) setZenMode(false);
          break;
```

Actualizar `shortcutsRef.current`. Localizar:

```js
shortcutsRef.current = { session, activeMode, zenMode, timerPanelProps, handleNudge, setActiveMode, setZenMode };
```

y reemplazarla por:

```js
shortcutsRef.current = { session, activeMode, zenMode, activePanel, timerPanelProps, handleNudge, setActiveMode, setZenMode, setActivePanel };
```

- [ ] **Step 5: Pass announce to both TimerPanel instances**

En el TimerPanel del overlay zen (`zenMode && ...`), anadir la prop:

```jsx
announce
```

En el TimerPanel del `practice-workspace`, anadir:

```jsx
announce={!zenMode}
```

- [ ] **Step 6: Build**

Run: `pnpm build`
Expected: sin errores.

- [ ] **Step 7: Manual verification**

Abrir un panel con un boton del topbar; cerrarlo con `Escape`: el foco vuelve a ese boton. Cerrarlo con el boton "Cerrar" interno: el foco vuelve al boton del topbar. Re-pulsar el mismo boton del topbar tambien cierra y deja el foco ahi. En modo Practicar con zen activo y una cue con instruccion, la instruccion se anuncia una sola vez (no doble).

- [ ] **Step 8: Commit**

```bash
git add src/App.jsx
git commit -m "feat(a11y): restore focus on panel close, Escape closes panel, single live region"
```

---

## Task 9: TimerPanel — toggle de cues y region aria-live (B8, B9, GAP-2, GAP-7)

**Files:**
- Modify: `src/components/TimerPanel.jsx`

- [ ] **Step 1: Accept the announce prop**

En la firma de `TimerPanel`, anadir `announce = false` a la destructuracion de props (junto a `showSoundNames`, etc.):

```js
export function TimerPanel({ session, onStart, onPause, onResume, onStop, onNudge, onZen, zenMode = false, presetName, playingCueName, playingInstruction, showSoundNames = true, announce = false, cues, playingCueId, cuesVisible, onToggleCues, onEditCues }) {
```

- [ ] **Step 2: Conditional aria-live on the instruction label**

Reemplazar el `<div className={...playing-instruction-label...}>` por:

```jsx
<div
  className={`playing-instruction-label${playingInstruction ? ' visible' : ''}`}
  aria-live={announce ? 'polite' : undefined}
>
  {playingInstruction}
</div>
```

(El nombre del sonido, `.playing-cue-label`, NO lleva `aria-live` por decision deliberada — GAP-7.)

- [ ] **Step 3: aria-expanded on the cues toggle**

En el boton de mostrar/ocultar cues, anadir `aria-expanded`:

```jsx
<button type="button" className="icon-button small" aria-expanded={cuesVisible} title={cuesVisible ? t('timer.hideCues') : t('timer.showCues')} onClick={onToggleCues}>
  {cuesVisible ? <ChevronUp size={15} /> : <ChevronDown size={15} />}
</button>
```

- [ ] **Step 4: Build**

Run: `pnpm build`
Expected: sin errores.

- [ ] **Step 5: Manual verification**

En Practicar (sin zen), al disparar una cue con instruccion, un lector de pantalla la anuncia una vez. El toggle de cues anuncia su estado expandido/colapsado.

- [ ] **Step 6: Commit**

```bash
git add src/components/TimerPanel.jsx
git commit -m "feat(a11y): aria-expanded cues toggle, conditional live region for instruction"
```

---

## Task 10: CSS — focus-visible, reduced-motion, contraste, h1 (A1, GAP-5, C10, C11, C13)

**Files:**
- Modify: `src/styles.css`

- [ ] **Step 1: Fix the --faint token**

En `:root`, cambiar:

```css
  --faint: #77736b;
```

por:

```css
  --faint: #8f8b82;
```

- [ ] **Step 2: Remove the outline:none that would defeat focus-visible**

Dos reglas existentes anulan el anillo nuevo por especificidad (`.cue-inspector input...` es (0,1,1) y gana siempre; `.preset-name-input` empata y gana por orden). Quitar el `outline: none;` de ambas. Es seguro: con `:focus-visible` el contorno solo aparece en foco de teclado, no al click (que era la razon original de suprimirlo).

En `.preset-name-input` (`styles.css:253`), borrar la linea:

```css
  outline: none;
```

En la regla `.cue-inspector input, .cue-inspector select, .cue-inspector textarea` (`styles.css:851`), borrar la linea:

```css
  outline: none;
```

- [ ] **Step 3: Add the global focus-visible ring**

Anadir (cerca del bloque `button { color: inherit; }` al principio):

```css
:where(button, [role="button"], [role="slider"], a, input, select, textarea):focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

/* Timeline lives inside an overflow:hidden panel; use an inner ring so the
   focus indicator is not clipped at the edges (GAP-5). */
.cue-marker:focus-visible,
.playhead:focus-visible,
.track-row:focus-visible {
  outline-offset: -2px;
}
```

- [ ] **Step 4: Add the h1 brand style**

Anadir (junto a las reglas de `.brand`):

```css
.brand-title {
  margin: 0;
  font-size: inherit;
  font-weight: inherit;
  letter-spacing: inherit;
  line-height: 1;
}
```

- [ ] **Step 5: Add the prefers-reduced-motion block**

Anadir al final del archivo:

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    transition-duration: 0.01ms !important;
    animation-duration: 0.01ms !important;
  }
}
```

- [ ] **Step 6: Build**

Run: `pnpm build`
Expected: sin errores.

- [ ] **Step 7: Manual verification**

Tab por toda la app: cada control muestra un anillo de foco ambar visible; en el timeline (markers, playhead, filas) el anillo no se recorta. El brand "Sadhana" se ve igual que antes (tamano/peso). Con `prefers-reduced-motion` activado en el SO (o DevTools > Rendering > Emulate CSS prefers-reduced-motion), las cues/instrucciones aparecen sin fade y las barras de progreso saltan sin animacion. El texto tenue (placeholders, separador "/") se lee con mas contraste.

- [ ] **Step 8: Commit**

```bash
git add src/styles.css
git commit -m "feat(a11y): focus-visible rings, reduced-motion, AA faint, h1 style"
```

---

## Verificacion final

- [ ] **Run the full test suite**

Run: `pnpm exec vitest run`
Expected: todos verdes (88 baseline + 10 nuevos de `keyboard.test.js` = 98).

- [ ] **Production build**

Run: `pnpm build`
Expected: sin errores.

- [ ] **Manual a11y checklist** (de la spec)

  - [ ] Tab recorre todos los controles con foco visible (sin recorte en el timeline).
  - [ ] Markers y playhead operables con flechas/Home/End.
  - [ ] `Space` sobre un marker selecciona la cue (no play/pause).
  - [ ] Atajos de modo (1/2/3/f) siguen funcionando aunque un boton tenga foco.
  - [ ] Paneles: foco entra al abrir; Escape, boton X y re-pulsar cierran y devuelven foco al disparador.
  - [ ] Modo activo, paneles, toggle de cues y swatch/marker/fila seleccionados anunciados por estado, no solo color.
  - [ ] Instruccion anunciada una sola vez en zen + practice.
  - [ ] `prefers-reduced-motion` activo: sin fades ni animaciones.

- [ ] **Update state.md** con la entrada de History correspondiente.
