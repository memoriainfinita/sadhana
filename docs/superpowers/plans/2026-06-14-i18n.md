# i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sistema i18n casero para la UI de Sadhana, multi-idioma desde el inicio, con es/en completos y pali parcial (easter egg).

**Architecture:** Diccionarios indexados por codigo de idioma + resolver puro `translate()` + `LanguageContext`/`useT()`. La logica (resolver, formateo Intl, seleccion de idioma) son funciones puras con TDD; el provider y los componentes se cablean y se verifican a mano en el navegador, siguiendo el patron del repo (sin jsdom, sin tests de componentes React).

**Tech Stack:** React 19, Vite 6, Vitest 3 (entorno node, funciones puras), `Intl.PluralRules` / `Intl.RelativeTimeFormat` / `Intl.DateTimeFormat`.

**Spec:** `docs/superpowers/specs/2026-06-14-i18n-design.md`

---

## File Structure

Nuevos:
- `src/i18n/translate.js` — resolver puro: navegacion de claves, fallback, interpolacion, pluralizacion, miss.
- `src/i18n/translate.test.js`
- `src/i18n/format.js` — `formatDuration`, `formatRelativeDate` (Intl).
- `src/i18n/format.test.js`
- `src/i18n/index.js` — `LANGUAGES` (metadata), `BASE_LANG`, `pickInitialLang`, `resolveDir`.
- `src/i18n/index.test.js`
- `src/i18n/locales/es.js`, `en.js`, `pi.js` — diccionarios.
- `src/i18n/locales/locales.test.js` — integridad (en cubre es).
- `src/i18n/LanguageContext.jsx` — provider + `useLang`.
- `src/i18n/useT.js` — hook `useT()` -> `t(key, vars)`.

Modificados:
- `src/domain/storage.js` — `STORAGE_KEYS.lang`.
- `src/main.jsx` — montar `LanguageProvider`.
- Los 10 componentes + `App.jsx` — sustituir strings por `t()`.
- `src/components/RecentSessions.jsx` + `App.jsx` — modelo de sesion crudo + formateo locale-aware.
- `index.html` — `lang` inicial (se mantiene `es`).

## Design decisions locked

- **Sin jsdom.** TDD solo en dominio puro. Provider/componentes verificados a mano.
- **Datos del usuario no se traducen.** Una vez cargado un preset, sus cues (nombre, instruccion) son datos editables del usuario: NO pasan por `t()`. Solo se traduce el **nombre del preset por defecto** en la biblioteca, via `t('presets.<id>')` con fallback a `preset.name`.
- **Modelo de sesion migra a datos crudos** (`durationSeconds`, `createdAt`, `cueName`). Sesiones viejas (con `duration`/`when` string) se muestran con fallback al string viejo.
- **`t(key, vars)`**: si la clave no existe en el idioma activo, cae a `es`; si tampoco existe, devuelve la clave cruda (+`console.warn` en dev).

---

## Task 1: Resolver puro `translate()`

**Files:**
- Create: `src/i18n/translate.js`
- Test: `src/i18n/translate.test.js`

- [ ] **Step 1: Write the failing test**

```js
// src/i18n/translate.test.js
import { describe, expect, test, vi } from 'vitest';
import { translate } from './translate.js';

const locales = {
  es: {
    modes: { practice: 'Practicar' },
    library: { cueCount: { one: '{n} senal', other: '{n} senales' } },
    greet: 'Hola {name}',
  },
  en: {
    modes: { practice: 'Practice' },
    library: { cueCount: { one: '{n} cue', other: '{n} cues' } },
  },
};

describe('translate', () => {
  test('resolves a nested key in the active language', () => {
    expect(translate(locales, 'en', 'es', 'modes.practice')).toBe('Practice');
  });

  test('falls back to base language when key missing in active', () => {
    expect(translate(locales, 'en', 'es', 'greet', { name: 'A' })).toBe('Hola A');
  });

  test('interpolates {vars}', () => {
    expect(translate(locales, 'es', 'es', 'greet', { name: 'Ana' })).toBe('Hola Ana');
  });

  test('selects plural category via Intl.PluralRules', () => {
    expect(translate(locales, 'en', 'es', 'library.cueCount', { n: 1 })).toBe('1 cue');
    expect(translate(locales, 'en', 'es', 'library.cueCount', { n: 3 })).toBe('3 cues');
  });

  test('returns the raw key when missing everywhere', () => {
    const warn = vi.spyOn(console, 'warn').mockImplementation(() => {});
    expect(translate(locales, 'en', 'es', 'does.not.exist')).toBe('does.not.exist');
    warn.mockRestore();
  });

  test('plural key without n does not crash, falls back to other', () => {
    expect(translate(locales, 'en', 'es', 'library.cueCount')).toBe('{n} cues');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test -- --run src/i18n/translate.test.js`
Expected: FAIL — `translate` is not defined.

- [ ] **Step 3: Write minimal implementation**

```js
// src/i18n/translate.js
function getPath(dict, key) {
  return key.split('.').reduce((node, part) => (node == null ? undefined : node[part]), dict);
}

function interpolate(template, vars) {
  return template.replace(/\{(\w+)\}/g, (match, name) =>
    Object.prototype.hasOwnProperty.call(vars, name) ? String(vars[name]) : match
  );
}

// Resolves key against the active language, falling back to baseLang, then to
// the raw key (with a dev warning). Supports {var} interpolation and plural
// objects ({ one, other, ... }) selected via Intl.PluralRules.
export function translate(locales, lang, baseLang, key, vars = {}) {
  let value = getPath(locales[lang], key);
  if (value === undefined) value = getPath(locales[baseLang], key);

  if (value === undefined) {
    if (import.meta.env?.DEV) console.warn(`[i18n] missing key: ${key}`);
    return key;
  }

  if (value && typeof value === 'object') {
    const n = Number(vars.n);
    const category = Number.isFinite(n) ? new Intl.PluralRules(lang).select(n) : 'other';
    value = value[category] ?? value.other;
  }

  return typeof value === 'string' ? interpolate(value, vars) : key;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test -- --run src/i18n/translate.test.js`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add src/i18n/translate.js src/i18n/translate.test.js
git commit -m "feat(i18n): pure translate resolver with fallback, interpolation, plurals"
```

---

## Task 2: Locale-aware formatting helpers

**Files:**
- Create: `src/i18n/format.js`
- Test: `src/i18n/format.test.js`

- [ ] **Step 1: Write the failing test**

```js
// src/i18n/format.test.js
import { describe, expect, test } from 'vitest';
import { formatDuration, formatRelativeDate } from './format.js';

describe('formatDuration', () => {
  test('formats whole minutes', () => {
    expect(formatDuration(1200, 'es')).toBe('20 min');
    expect(formatDuration(1200, 'en')).toBe('20 min');
  });

  test('rounds to nearest minute', () => {
    expect(formatDuration(90, 'es')).toBe('2 min');
  });
});

describe('formatRelativeDate', () => {
  const now = new Date('2026-06-14T12:00:00.000Z');

  test('formats same-day as relative hours in es', () => {
    const earlier = new Date('2026-06-14T10:00:00.000Z');
    expect(formatRelativeDate(earlier, 'es', now)).toContain('hace');
  });

  test('formats days ago in en', () => {
    const past = new Date('2026-06-12T12:00:00.000Z');
    expect(formatRelativeDate(past, 'en', now)).toBe('2 days ago');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test -- --run src/i18n/format.test.js`
Expected: FAIL — module not found.

- [ ] **Step 3: Write minimal implementation**

```js
// src/i18n/format.js

// Whole-minute duration. "min" is the same token in es/en; other locales can
// override later via a key if needed.
export function formatDuration(seconds, lang) {
  const minutes = Math.max(1, Math.round(seconds / 60));
  return `${new Intl.NumberFormat(lang).format(minutes)} min`;
}

// Relative date ("hace 2 dias" / "2 days ago") via Intl.RelativeTimeFormat,
// choosing the coarsest sensible unit.
export function formatRelativeDate(date, lang, now = new Date()) {
  const d = date instanceof Date ? date : new Date(date);
  const diffSeconds = Math.round((d.getTime() - now.getTime()) / 1000);
  const rtf = new Intl.RelativeTimeFormat(lang, { numeric: 'auto' });
  const abs = Math.abs(diffSeconds);

  if (abs < 3600) return rtf.format(Math.round(diffSeconds / 60), 'minute');
  if (abs < 86400) return rtf.format(Math.round(diffSeconds / 3600), 'hour');
  return rtf.format(Math.round(diffSeconds / 86400), 'day');
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test -- --run src/i18n/format.test.js`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/i18n/format.js src/i18n/format.test.js
git commit -m "feat(i18n): locale-aware duration and relative-date helpers"
```

---

## Task 3: Language registry and selection

**Files:**
- Create: `src/i18n/index.js`
- Test: `src/i18n/index.test.js`

- [ ] **Step 1: Write the failing test**

```js
// src/i18n/index.test.js
import { describe, expect, test } from 'vitest';
import { LANGUAGES, BASE_LANG, pickInitialLang, resolveDir } from './index.js';

describe('language registry', () => {
  test('base language is es and is registered', () => {
    expect(BASE_LANG).toBe('es');
    expect(LANGUAGES.some((l) => l.code === 'es')).toBe(true);
  });

  test('resolveDir returns rtl for ar, ltr otherwise', () => {
    expect(resolveDir('ar')).toBe('rtl');
    expect(resolveDir('es')).toBe('ltr');
    expect(resolveDir('unknown')).toBe('ltr');
  });
});

describe('pickInitialLang', () => {
  test('prefers a valid stored value', () => {
    expect(pickInitialLang('en', 'fr-FR')).toBe('en');
  });

  test('falls back to navigator language prefix when no stored value', () => {
    expect(pickInitialLang(null, 'en-US')).toBe('en');
  });

  test('falls back to base when nothing matches', () => {
    expect(pickInitialLang(null, 'xx-XX')).toBe('es');
  });

  test('ignores an unregistered stored value', () => {
    expect(pickInitialLang('zz', 'en-US')).toBe('en');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test -- --run src/i18n/index.test.js`
Expected: FAIL — module not found.

- [ ] **Step 3: Write minimal implementation**

```js
// src/i18n/index.js
// Implemented (have a dictionary): es, en, pi. The rest are registered so the
// selector lists them; their dictionaries are added later (each falls back to es).
export const LANGUAGES = [
  { code: 'es', native: 'Espanol', dir: 'ltr' },
  { code: 'en', native: 'English', dir: 'ltr' },
  { code: 'fr', native: 'Francais', dir: 'ltr' },
  { code: 'de', native: 'Deutsch', dir: 'ltr' },
  { code: 'it', native: 'Italiano', dir: 'ltr' },
  { code: 'pt', native: 'Portugues', dir: 'ltr' },
  { code: 'nl', native: 'Nederlands', dir: 'ltr' },
  { code: 'pl', native: 'Polski', dir: 'ltr' },
  { code: 'el', native: 'Ellinika', dir: 'ltr' },
  { code: 'ru', native: 'Russkiy', dir: 'ltr' },
  { code: 'zh', native: 'Zhongwen', dir: 'ltr' },
  { code: 'ja', native: 'Nihongo', dir: 'ltr' },
  { code: 'hi', native: 'Hindi', dir: 'ltr' },
  { code: 'si', native: 'Sinhala', dir: 'ltr' },
  { code: 'ar', native: 'al-Arabiyya', dir: 'rtl' },
  { code: 'pi', native: 'Pali', dir: 'ltr' },
];

export const BASE_LANG = 'es';

const CODES = new Set(LANGUAGES.map((l) => l.code));

export function resolveDir(lang) {
  return LANGUAGES.find((l) => l.code === lang)?.dir ?? 'ltr';
}

// Stored choice wins; else match navigator language prefix; else base.
export function pickInitialLang(stored, navigatorLang) {
  if (stored && CODES.has(stored)) return stored;
  const prefix = (navigatorLang ?? '').slice(0, 2).toLowerCase();
  if (CODES.has(prefix)) return prefix;
  return BASE_LANG;
}
```

Note: only `es`/`en`/`pi` have dictionaries in Task 4. Selecting an unimplemented language falls back to `es` per-key — acceptable for fase B (the selector still works; translations fill in later).

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test -- --run src/i18n/index.test.js`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add src/i18n/index.js src/i18n/index.test.js
git commit -m "feat(i18n): language registry, dir resolution, initial-language pick"
```

---

## Task 4: Dictionaries (es, en, pi) + integrity test

**Files:**
- Create: `src/i18n/locales/es.js`, `src/i18n/locales/en.js`, `src/i18n/locales/pi.js`
- Test: `src/i18n/locales/locales.test.js`

This `es.js` is the **key contract** for every migration task below. `en.js` mirrors every key. `pi.js` is partial (filled in Task 15).

- [ ] **Step 1: Write the failing integrity test**

```js
// src/i18n/locales/locales.test.js
import { describe, expect, test } from 'vitest';
import es from './es.js';
import en from './en.js';

function keyPaths(obj, prefix = '') {
  return Object.entries(obj).flatMap(([k, v]) => {
    const path = prefix ? `${prefix}.${k}` : k;
    // plural objects ({ one, other }) are leaves
    if (v && typeof v === 'object' && !('other' in v)) return keyPaths(v, path);
    return [path];
  });
}

describe('locale integrity', () => {
  test('en covers every key in es (es is canonical)', () => {
    const esKeys = keyPaths(es);
    const enKeys = new Set(keyPaths(en));
    const missing = esKeys.filter((k) => !enKeys.has(k));
    expect(missing).toEqual([]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test -- --run src/i18n/locales/locales.test.js`
Expected: FAIL — modules not found.

- [ ] **Step 3: Write `es.js` (canonical contract)**

```js
// src/i18n/locales/es.js
export default {
  app: { title: 'Sadhana' },

  modes: { navLabel: 'Modos de Sadhana', practice: 'Practicar', design: 'Disenar', remember: 'Recordar' },

  shell: {
    brand: 'Sadhana',
    globalControls: 'Controles globales',
    theme: 'Tema', themeTitle: 'Tema y color de acento',
    audio: 'Audio', audioTitle: 'Volumen y silencio',
    settings: 'Configuracion', settingsTitle: 'Exportar / importar datos',
  },

  panel: { header: 'Panel', close: 'Cerrar', closeTitle: 'Cerrar panel' },

  theme: {
    heading: 'Tema',
    useContrast: 'Usar contraste alto', useDim: 'Usar tema sobrio',
    toggleTitle: 'Cambiar entre tema sobrio y alto contraste',
    accentLabel: 'Color de acento', customColor: 'Color personalizado',
    accents: { amber: 'Ambar', gold: 'Oro', sage: 'Salvia', cyan: 'Cian', rose: 'Rosa', lilac: 'Lila' },
  },

  audio: {
    heading: 'Audio', masterVolume: 'Volumen maestro', mute: 'Silenciar',
    stop: 'Detener audio', stopTitle: 'Detener todo el audio en reproduccion ahora mismo',
  },

  settings: {
    heading: 'Configuracion',
    showSoundNames: 'Mostrar nombre del sonido al dispararse',
    export: 'Exportar datos', exportTitle: 'Copiar presets y sesiones al portapapeles como JSON',
    importPlaceholder: 'Pega aqui un JSON exportado',
    language: 'Idioma',
  },

  playback: {
    back: 'Retroceder 15s', backTitle: 'Retroceder 15 segundos',
    pause: 'Pausar', pauseTitle: 'Pausar reproduccion',
    play: 'Reproducir', startTitle: 'Iniciar reproduccion', resumeTitle: 'Reanudar reproduccion',
    forward: 'Avanzar 15s', forwardTitle: 'Avanzar 15 segundos',
    stop: 'Detener', stopTitle: 'Detener y guardar sesion',
  },

  timer: {
    panelLabel: 'Temporizador de practica',
    zenEnter: 'Modo zen', zenExit: 'Salir del modo zen',
    zenEnterTitle: 'Modo zen — pantalla completa', zenExitTitle: 'Salir del modo zen',
    back: 'Retroceder 15 segundos', forward: 'Avanzar 15 segundos',
    pauseSession: 'Pausar sesion',
    startResume: 'Iniciar o reanudar sesion', startTitle: 'Iniciar sesion', resumeTitle: 'Reanudar sesion',
    stop: 'Detener', stopTitle: 'Detener sesion y guardar en Recordar',
    hideCues: 'Ocultar cues', showCues: 'Mostrar cues', editCues: 'Editar cues',
  },

  timeline: {
    panelLabel: 'Linea de tiempo', heading: 'Linea de tiempo',
    add: 'Anadir', addTitle: 'Anadir nueva cue en la posicion actual',
    undo: 'Deshacer', undoTitle: 'Deshacer ultimo cambio',
    search: 'Buscar', searchTitle: 'Buscar cue por nombre',
    duration: 'Duracion', min: 'min',
    searchLabel: 'Buscar cue', searchPlaceholder: 'Nombre de cue',
    cueAria: 'Cue {name}',
  },

  inspector: {
    aside: 'Editor de cue', heading: 'Cue',
    duplicate: 'Duplicar cue', duplicateTitle: 'Duplicar esta cue',
    delete: 'Eliminar cue', deleteTitle: 'Eliminar esta cue',
    name: 'Nombre', sound: 'Sonido',
    preview: 'Previsualizar sonido', previewTitle: 'Reproducir sonido de esta cue',
    stopPreview: 'Parar preview', stopPreviewTitle: 'Parar reproduccion del sonido',
    instruction: 'Instruccion', instructionPlaceholder: 'ej. Inhala, Reten, Exhala...',
    visibility: 'Visibilidad (s)', time: 'Tiempo', volume: 'Volumen',
    durationField: 'Dur. (s)', fadeIn: 'Fade in (s)', fadeOut: 'Fade out (s)',
    notes: 'Notas', notesPlaceholder: 'Anade notas sobre este cue...',
  },

  sounds: { groups: { bells: 'Campanas', ambient: 'Ambiente', fx: 'Efectos' } },

  library: {
    heading: 'Presets', empty: 'Aun no tienes presets. Crea uno desde Disenar y guardalo.',
    cueCount: { one: '{n} cue', other: '{n} cues' },
    durationMin: '{min} min',
    load: 'Cargar', loadTitle: 'Cargar preset: {name}',
    delete: 'Eliminar', deleteTitle: 'Eliminar preset: {name}',
  },

  presets: {
    'default-yoga-nidra': 'Yoga Nidra',
    'default-pranayama-478': 'Pranayama 4-7-8',
    'default-meditacion-matutina': 'Meditacion matutina',
    'default-relajacion-profunda': 'Relajacion profunda',
    'default-caminata-consciente': 'Caminata consciente',
  },

  sessions: {
    heading: 'Ultimas sesiones', sectionLabel: 'Ultimas sesiones', viewAll: 'Ver todo',
    empty: 'Aun no has completado ninguna sesion. Practica y apareceran aqui.',
    repeat: 'Repetir', repeatTitle: 'Ir a Practicar con la sesion: {name}',
    delete: 'Eliminar', deleteTitle: 'Eliminar sesion: {name}',
    optionsAria: 'Opciones de {name}', repeatAria: 'Repetir {name}', deleteAria: 'Eliminar {name}',
    defaultName: 'Sesion completada', withCue: 'Sesion con {name}',
  },

  presetBar: {
    placeholder: 'Nombre del preset', nameAria: 'Nombre del preset',
    save: 'Guardar preset', saveTitle: 'Guardar preset con las cues actuales',
  },

  notices: {
    cueAdded: 'Cue anadida', cueDuplicated: 'Cue duplicada', cueDeleted: 'Cue eliminada',
    changeUndone: 'Cambio deshecho',
    presetSaved: 'Preset guardado', presetLoaded: 'Preset cargado: {name}', presetDeleted: 'Preset eliminado',
    sessionSaved: 'Sesion guardada en Recordar', sessionDeleted: 'Sesion eliminada',
    repeat: 'Repetir: {name}', durationUpdated: 'Duracion actualizada',
    dataCopied: 'Datos copiados al portapapeles', dataImported: 'Datos importados', invalidJson: 'JSON no valido',
  },
};
```

- [ ] **Step 4: Write `en.js` (mirror every key)**

```js
// src/i18n/locales/en.js
export default {
  app: { title: 'Sadhana' },
  modes: { navLabel: 'Sadhana modes', practice: 'Practice', design: 'Design', remember: 'Remember' },
  shell: {
    brand: 'Sadhana', globalControls: 'Global controls',
    theme: 'Theme', themeTitle: 'Theme and accent color',
    audio: 'Audio', audioTitle: 'Volume and mute',
    settings: 'Settings', settingsTitle: 'Export / import data',
  },
  panel: { header: 'Panel', close: 'Close', closeTitle: 'Close panel' },
  theme: {
    heading: 'Theme', useContrast: 'Use high contrast', useDim: 'Use dim theme',
    toggleTitle: 'Switch between dim and high-contrast theme',
    accentLabel: 'Accent color', customColor: 'Custom color',
    accents: { amber: 'Amber', gold: 'Gold', sage: 'Sage', cyan: 'Cyan', rose: 'Rose', lilac: 'Lilac' },
  },
  audio: {
    heading: 'Audio', masterVolume: 'Master volume', mute: 'Mute',
    stop: 'Stop audio', stopTitle: 'Stop all playing audio right now',
  },
  settings: {
    heading: 'Settings', showSoundNames: 'Show sound name when it triggers',
    export: 'Export data', exportTitle: 'Copy presets and sessions to clipboard as JSON',
    importPlaceholder: 'Paste exported JSON here', language: 'Language',
  },
  playback: {
    back: 'Back 15s', backTitle: 'Back 15 seconds',
    pause: 'Pause', pauseTitle: 'Pause playback',
    play: 'Play', startTitle: 'Start playback', resumeTitle: 'Resume playback',
    forward: 'Forward 15s', forwardTitle: 'Forward 15 seconds',
    stop: 'Stop', stopTitle: 'Stop and save session',
  },
  timer: {
    panelLabel: 'Practice timer',
    zenEnter: 'Zen mode', zenExit: 'Exit zen mode',
    zenEnterTitle: 'Zen mode — fullscreen', zenExitTitle: 'Exit zen mode',
    back: 'Back 15 seconds', forward: 'Forward 15 seconds',
    pauseSession: 'Pause session',
    startResume: 'Start or resume session', startTitle: 'Start session', resumeTitle: 'Resume session',
    stop: 'Stop', stopTitle: 'Stop session and save to Remember',
    hideCues: 'Hide cues', showCues: 'Show cues', editCues: 'Edit cues',
  },
  timeline: {
    panelLabel: 'Timeline', heading: 'Timeline',
    add: 'Add', addTitle: 'Add a new cue at the current position',
    undo: 'Undo', undoTitle: 'Undo last change',
    search: 'Search', searchTitle: 'Search cue by name',
    duration: 'Duration', min: 'min',
    searchLabel: 'Search cue', searchPlaceholder: 'Cue name',
    cueAria: 'Cue {name}',
  },
  inspector: {
    aside: 'Cue editor', heading: 'Cue',
    duplicate: 'Duplicate cue', duplicateTitle: 'Duplicate this cue',
    delete: 'Delete cue', deleteTitle: 'Delete this cue',
    name: 'Name', sound: 'Sound',
    preview: 'Preview sound', previewTitle: "Play this cue's sound",
    stopPreview: 'Stop preview', stopPreviewTitle: 'Stop sound playback',
    instruction: 'Instruction', instructionPlaceholder: 'e.g. Inhale, Hold, Exhale...',
    visibility: 'Visibility (s)', time: 'Time', volume: 'Volume',
    durationField: 'Dur. (s)', fadeIn: 'Fade in (s)', fadeOut: 'Fade out (s)',
    notes: 'Notes', notesPlaceholder: 'Add notes about this cue...',
  },
  sounds: { groups: { bells: 'Bells', ambient: 'Ambient', fx: 'Effects' } },
  library: {
    heading: 'Presets', empty: "You don't have presets yet. Create one from Design and save it.",
    cueCount: { one: '{n} cue', other: '{n} cues' },
    durationMin: '{min} min',
    load: 'Load', loadTitle: 'Load preset: {name}',
    delete: 'Delete', deleteTitle: 'Delete preset: {name}',
  },
  presets: {
    'default-yoga-nidra': 'Yoga Nidra',
    'default-pranayama-478': 'Pranayama 4-7-8',
    'default-meditacion-matutina': 'Morning meditation',
    'default-relajacion-profunda': 'Deep relaxation',
    'default-caminata-consciente': 'Mindful walking',
  },
  sessions: {
    heading: 'Recent sessions', sectionLabel: 'Recent sessions', viewAll: 'View all',
    empty: "You haven't completed any session yet. Practice and they'll show up here.",
    repeat: 'Repeat', repeatTitle: 'Go to Practice with the session: {name}',
    delete: 'Delete', deleteTitle: 'Delete session: {name}',
    optionsAria: 'Options for {name}', repeatAria: 'Repeat {name}', deleteAria: 'Delete {name}',
    defaultName: 'Completed session', withCue: 'Session with {name}',
  },
  presetBar: {
    placeholder: 'Preset name', nameAria: 'Preset name',
    save: 'Save preset', saveTitle: 'Save preset with the current cues',
  },
  notices: {
    cueAdded: 'Cue added', cueDuplicated: 'Cue duplicated', cueDeleted: 'Cue deleted',
    changeUndone: 'Change undone',
    presetSaved: 'Preset saved', presetLoaded: 'Preset loaded: {name}', presetDeleted: 'Preset deleted',
    sessionSaved: 'Session saved to Remember', sessionDeleted: 'Session deleted',
    repeat: 'Repeat: {name}', durationUpdated: 'Duration updated',
    dataCopied: 'Data copied to clipboard', dataImported: 'Data imported', invalidJson: 'Invalid JSON',
  },
};
```

- [ ] **Step 5: Write `pi.js` (start empty; filled in Task 15)**

```js
// src/i18n/locales/pi.js
// Easter egg: partial. Only keys with a natural Pali (IAST) term; the rest fall
// back to es. Filled in Task 15.
export default {};
```

- [ ] **Step 6: Run integrity test**

Run: `pnpm test -- --run src/i18n/locales/locales.test.js`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/i18n/locales/
git commit -m "feat(i18n): es/en dictionaries with integrity test, empty pi stub"
```

---

## Task 5: Provider, hook, storage key, mount

**Files:**
- Modify: `src/domain/storage.js:1-6` (add `lang` key)
- Create: `src/i18n/LanguageContext.jsx`, `src/i18n/useT.js`
- Modify: `src/main.jsx`

- [ ] **Step 1: Add storage key**

In `src/domain/storage.js`, extend `STORAGE_KEYS`:

```js
export const STORAGE_KEYS = {
  presets: 'sadhana-next.presets',
  sessions: 'sadhana-next.sessions',
  showSoundNames: 'sadhana-next.showSoundNames',
  examplesCleaned: 'sadhana-next.examplesCleaned',
  lang: 'sadhana-next.lang',
};
```

- [ ] **Step 2: Create the context/provider**

```jsx
// src/i18n/LanguageContext.jsx
import { createContext, useContext, useEffect, useState } from 'react';
import { BASE_LANG, pickInitialLang, resolveDir } from './index.js';
import { STORAGE_KEYS } from '../domain/storage.js';
import es from './locales/es.js';
import en from './locales/en.js';
import pi from './locales/pi.js';
import { translate } from './translate.js';

const LOCALES = { es, en, pi };

const LanguageContext = createContext(null);

export function LanguageProvider({ children }) {
  const [lang, setLangState] = useState(() =>
    pickInitialLang(window.localStorage.getItem(STORAGE_KEYS.lang), navigator.language)
  );

  useEffect(() => {
    const root = document.documentElement;
    root.lang = lang;
    root.dir = resolveDir(lang);
    document.title = translate(LOCALES, lang, BASE_LANG, 'app.title');
  }, [lang]);

  function setLang(next) {
    window.localStorage.setItem(STORAGE_KEYS.lang, next);
    setLangState(next);
  }

  const t = (key, vars) => translate(LOCALES, lang, BASE_LANG, key, vars);

  return (
    <LanguageContext.Provider value={{ lang, setLang, t }}>
      {children}
    </LanguageContext.Provider>
  );
}

export function useLang() {
  const ctx = useContext(LanguageContext);
  if (!ctx) throw new Error('useLang must be used within LanguageProvider');
  return ctx;
}
```

- [ ] **Step 3: Create the `useT` hook**

```js
// src/i18n/useT.js
import { useLang } from './LanguageContext.jsx';

// Returns t(key, vars). Components that also need the active language or the
// setter use useLang() directly.
export function useT() {
  return useLang().t;
}
```

- [ ] **Step 4: Mount the provider in main.jsx**

Wrap `<App />`:

```jsx
import { LanguageProvider } from './i18n/LanguageContext.jsx';
// ...
createRoot(root).render(
  <React.StrictMode>
    <LanguageProvider>
      <App />
    </LanguageProvider>
  </React.StrictMode>
);
```

- [ ] **Step 5: Verify in browser**

Run: `pnpm dev` (Bash `run_in_background`).
Open the app. Expected: it renders unchanged (no component uses `t()` yet). In DevTools console: `document.documentElement.lang` reflects browser language if es/en/pi, else `es`. No errors.

- [ ] **Step 6: Commit**

```bash
git add src/domain/storage.js src/i18n/LanguageContext.jsx src/i18n/useT.js src/main.jsx
git commit -m "feat(i18n): language provider, useT hook, mount in app root"
```

---

## Task 6: Language selector in Settings + migrate GlobalPanel

**Files:**
- Modify: `src/components/GlobalPanel.jsx`

GlobalPanel is the proof component: it gets the selector and is fully migrated to `t()`.

- [ ] **Step 1: Migrate GlobalPanel strings and add the selector**

- Import: `import { useT } from '../i18n/useT.js';` and `import { useLang } from '../i18n/LanguageContext.jsx';` and `import { LANGUAGES } from '../i18n/index.js';`
- Inside the component: `const t = useT(); const { lang, setLang } = useLang();`
- Replace `ACCENT_PRESETS` labels with keys: change each `label` to a key id (`amber`, `gold`, `sage`, `cyan`, `rose`, `lilac`) and render `t('theme.accents.' + preset.key)`.
- Replace every visible string, `aria-label`, and `title` using this mapping:

| Current literal | Key |
|---|---|
| `Panel` | `panel.header` |
| `Cerrar` / title `Cerrar panel` | `panel.close` / `panel.closeTitle` |
| `Tema` (h2) | `theme.heading` |
| `Usar contraste alto` / `Usar tema sobrio` | `theme.useContrast` / `theme.useDim` |
| title `Cambiar entre...` | `theme.toggleTitle` |
| `Color de acento` | `theme.accentLabel` |
| `Color personalizado` | `theme.customColor` |
| `Audio` (h2) | `audio.heading` |
| `Volumen maestro` | `audio.masterVolume` |
| `Silenciar` | `audio.mute` |
| `Detener audio` / title | `audio.stop` / `audio.stopTitle` |
| `Configuracion` (h2) | `settings.heading` |
| `Mostrar nombre del sonido...` | `settings.showSoundNames` |
| `Exportar datos` / title | `settings.export` / `settings.exportTitle` |
| placeholder `Pega aqui un JSON exportado` | `settings.importPlaceholder` |
| `aria-label="Panel global"` | use `t('shell.globalControls')` or keep literal `Panel` via `panel.header` |

- Add the selector inside the `settings` block, above export:

```jsx
<label className="settings-language">
  {t('settings.language')}
  <select value={lang} onChange={(event) => setLang(event.target.value)}>
    {LANGUAGES.map((l) => (
      <option key={l.code} value={l.code}>{l.native}</option>
    ))}
  </select>
</label>
```

- [ ] **Step 2: Verify in browser**

Run: app already on `pnpm dev`. Open the Settings panel.
Expected: selector lists all 16 languages by native name. Switching es<->en flips every label, button, title and the export placeholder in the panel live, with no reload. Switching to e.g. `fr` leaves the panel in Spanish (fallback) but persists the choice. Reload keeps the chosen language.

- [ ] **Step 3: Commit**

```bash
git add src/components/GlobalPanel.jsx
git commit -m "feat(i18n): language selector in Settings, GlobalPanel fully localized"
```

---

## Task 7: AppShell + ModeTabs

**Files:**
- Modify: `src/components/AppShell.jsx`, `src/components/ModeTabs.jsx`

- [ ] **Step 1: Migrate ModeTabs**

- Import `useT`. The `MODES` array keeps `id`/`icon` but drops `label`; render `t('modes.' + mode.id)` for the span. `nav` aria-label -> `t('modes.navLabel')`.

- [ ] **Step 2: Migrate AppShell**

- Import `useT`. Replace via mapping: brand aria/text -> `shell.brand`; topbar-actions aria -> `shell.globalControls`; the three icon buttons' `aria-label`/`title` -> `shell.theme`/`shell.themeTitle`, `shell.audio`/`shell.audioTitle`, `shell.settings`/`shell.settingsTitle`. The SVG `aria-label="Sadhana"` -> `shell.brand`.

- [ ] **Step 3: Verify in browser**

Switch language in Settings. Expected: mode tabs (Practice/Design/Remember) and topbar tooltips switch live.

- [ ] **Step 4: Commit**

```bash
git add src/components/AppShell.jsx src/components/ModeTabs.jsx
git commit -m "feat(i18n): localize AppShell and ModeTabs"
```

---

## Task 8: PlaybackBar

**Files:**
- Modify: `src/components/PlaybackBar.jsx`

- [ ] **Step 1: Migrate**

Import `useT`. Mapping (`aria-label` / `title`):

| Literal | Key |
|---|---|
| `Retroceder 15s` / `Retroceder 15 segundos` | `playback.back` / `playback.backTitle` |
| `Pausar` / `Pausar reproduccion` | `playback.pause` / `playback.pauseTitle` |
| `Reproducir` | `playback.play` |
| `Reanudar reproduccion` / `Iniciar reproduccion` | `playback.resumeTitle` / `playback.startTitle` |
| `Avanzar 15s` / `Avanzar 15 segundos` | `playback.forward` / `playback.forwardTitle` |
| `Detener` / `Detener y guardar sesion` | `playback.stop` / `playback.stopTitle` |

The play/pause title ternary becomes `title={isPaused ? t('playback.resumeTitle') : t('playback.startTitle')}`.

- [ ] **Step 2: Verify in browser**

Design mode, switch language. Expected: playback control tooltips switch.

- [ ] **Step 3: Commit**

```bash
git add src/components/PlaybackBar.jsx
git commit -m "feat(i18n): localize PlaybackBar"
```

---

## Task 9: TimerPanel

**Files:**
- Modify: `src/components/TimerPanel.jsx`

- [ ] **Step 1: Migrate**

Import `useT`. Mapping:

| Literal | Key |
|---|---|
| aria `Temporizador de practica` | `timer.panelLabel` |
| zen aria/title `Modo zen` / `Salir del modo zen` / `Modo zen — pantalla completa` | `timer.zenEnter` / `timer.zenExit` / `timer.zenEnterTitle` / `timer.zenExitTitle` |
| `Retroceder 15 segundos` / `Avanzar 15 segundos` (aria+title) | `timer.back` / `timer.forward` |
| `Pausar sesion` | `timer.pauseSession` |
| `Iniciar o reanudar sesion` (aria) | `timer.startResume` |
| `Reanudar sesion` / `Iniciar sesion` (title) | `timer.resumeTitle` / `timer.startTitle` |
| `Detener` (text) / `Detener sesion y guardar en Recordar` (title) | `timer.stop` / `timer.stopTitle` |
| `Ocultar cues` / `Mostrar cues` | `timer.hideCues` / `timer.showCues` |
| `Editar cues` | `timer.editCues` |

The `{Math.round(session.durationSeconds / 60)} min` label: leave as-is for now (duration label uses a local format; locale formatting of the readout is out of fase B since MM:SS is universal). The `min` token stays literal here — it is not user-language-sensitive in practice. (If desired later, swap to `t('timeline.min')`.)

- [ ] **Step 2: Verify in browser**

Practice mode, switch language. Expected: timer tooltips, Stop label, zen and cue toggles switch.

- [ ] **Step 3: Commit**

```bash
git add src/components/TimerPanel.jsx
git commit -m "feat(i18n): localize TimerPanel"
```

---

## Task 10: Timeline

**Files:**
- Modify: `src/components/Timeline.jsx`

- [ ] **Step 1: Migrate**

Import `useT` in `Timeline`. Mapping:

| Literal | Key |
|---|---|
| aria `Linea de tiempo` / h2 `Linea de tiempo` | `timeline.panelLabel` / `timeline.heading` |
| `Anadir` (text) / title | `timeline.add` / `timeline.addTitle` |
| `Deshacer` (aria) / title | `timeline.undo` / `timeline.undoTitle` |
| `Buscar` (aria) / title | `timeline.search` / `timeline.searchTitle` |
| `Duracion` / `min` | `timeline.duration` / `timeline.min` |
| `Buscar cue` (label) / placeholder `Nombre de cue` | `timeline.searchLabel` / `timeline.searchPlaceholder` |
| marker `aria-label={`Cue ${cue.name}`}` | `t('timeline.cueAria', { name: cue.name })` |

`TrackRow` has no user-facing strings — leave it.

- [ ] **Step 2: Verify in browser**

Design mode, switch language. Expected: timeline header, Add/Undo/Search tooltips, Duration label, search field switch.

- [ ] **Step 3: Commit**

```bash
git add src/components/Timeline.jsx
git commit -m "feat(i18n): localize Timeline"
```

---

## Task 11: CueInspector + sound group labels

**Files:**
- Modify: `src/components/CueInspector.jsx`, `src/domain/sounds.js`

- [ ] **Step 1: Expose the raw folder key from sound groups**

In `src/domain/sounds.js`, add the folder id to each group so the UI can translate it, keeping `group` as fallback:

```js
export const SOUND_OPTION_GROUPS = Object.entries(byGroup).map(([folder, options]) => ({
  folder,
  group: groupLabel(folder),
  options,
}));
```

- [ ] **Step 2: Migrate CueInspector strings**

Import `useT`. Mapping:

| Literal | Key |
|---|---|
| aside `Editor de cue` / h2 `Cue` | `inspector.aside` / `inspector.heading` |
| `Duplicar cue` / `Duplicar esta cue` | `inspector.duplicate` / `inspector.duplicateTitle` |
| `Eliminar cue` / `Eliminar esta cue` | `inspector.delete` / `inspector.deleteTitle` |
| `Nombre` | `inspector.name` |
| `Sonido` | `inspector.sound` |
| `Previsualizar sonido` / `Reproducir sonido de esta cue` | `inspector.preview` / `inspector.previewTitle` |
| `Parar preview` / `Parar reproduccion del sonido` | `inspector.stopPreview` / `inspector.stopPreviewTitle` |
| `Instruccion` / placeholder | `inspector.instruction` / `inspector.instructionPlaceholder` |
| `Visibilidad (s)` | `inspector.visibility` |
| `Tiempo` | `inspector.time` |
| `Volumen` | `inspector.volume` |
| `Dur. (s)` / `Fade in (s)` / `Fade out (s)` | `inspector.durationField` / `inspector.fadeIn` / `inspector.fadeOut` |
| `Notas` / placeholder | `inspector.notes` / `inspector.notesPlaceholder` |

- [ ] **Step 3: Translate sound group labels**

`optgroup` label becomes: `label={t('sounds.groups.' + group.folder)}`. Because `t` falls back to the raw key on miss, add a guard so unknown folders show the capitalized fallback rather than the raw key:

```jsx
<optgroup key={group.folder} label={t('sounds.groups.' + group.folder) === 'sounds.groups.' + group.folder ? group.group : t('sounds.groups.' + group.folder)}>
```

Simpler: compute once — `const groupLabel = t('sounds.groups.' + group.folder); const shown = groupLabel.startsWith('sounds.groups.') ? group.group : groupLabel;`

- [ ] **Step 4: Verify in browser**

Design mode, select a cue, switch language. Expected: all inspector labels switch; sound categories show as Bells/Ambient/Effects (en) or Campanas/Ambiente/Efectos (es); a folder without a key still shows its capitalized name.

- [ ] **Step 5: Commit**

```bash
git add src/components/CueInspector.jsx src/domain/sounds.js
git commit -m "feat(i18n): localize CueInspector and translatable sound groups"
```

---

## Task 12: PresetLibrary (plurals + default preset names)

**Files:**
- Modify: `src/domain/presets.js` (add `resolvePresetName`), `src/domain/presets.test.js`
- Modify: `src/components/PresetLibrary.jsx`

The default-preset-name resolution is shared by PresetLibrary (T12) and App (T14), so it lives in `domain/presets.js` as a pure, tested function — not duplicated in components.

- [ ] **Step 1: Write the failing test for `resolvePresetName`**

```js
// add to src/domain/presets.test.js
import { resolvePresetName } from './presets.js';

describe('resolvePresetName', () => {
  // t stub: returns translated name for known default ids, echoes the key otherwise
  const t = (key) => (key === 'presets.default-yoga-nidra' ? 'Yoga Nidra' : key);

  test('returns translated name for a default preset id', () => {
    expect(resolvePresetName({ id: 'default-yoga-nidra', name: 'X' }, t)).toBe('Yoga Nidra');
  });

  test('falls back to literal name for a user preset (key echoes back)', () => {
    expect(resolvePresetName({ id: 'abc-123', name: 'Mi preset' }, t)).toBe('Mi preset');
  });
});
```

- [ ] **Step 2: Run it, verify it fails**

Run: `pnpm test -- --run src/domain/presets.test.js`
Expected: FAIL — `resolvePresetName` is not defined.

- [ ] **Step 3: Implement in `src/domain/presets.js`**

```js
// Default presets are app content (translated by id); user presets show their
// literal name. t echoes the key back on miss, which is how we detect a user preset.
export function resolvePresetName(preset, t) {
  const key = `presets.${preset.id}`;
  const translated = t(key);
  return translated === key ? preset.name : translated;
}
```

- [ ] **Step 4: Run it, verify it passes**

Run: `pnpm test -- --run src/domain/presets.test.js`
Expected: PASS.

- [ ] **Step 5: Migrate PresetLibrary with plural and shared name resolver**

Import `useT` and `resolvePresetName`. Changes:
- aria `Presets guardados` and h2 -> `t('library.heading')` (aria can reuse heading).
- empty state -> `t('library.empty')`.
- card meta: replace `{cues} cues · {min} min` with `{t('library.cueCount', { n: preset.cues?.length ?? 0 })} · {t('library.durationMin', { min: presetMinutes(preset) })}`.
- preset display name: `const name = resolvePresetName(preset, t);` used for `<h3>` and inside `loadTitle`/`deleteTitle` (`t('library.loadTitle', { name })`).
- `Cargar` / `Eliminar` buttons -> `library.load` / `library.delete`.

- [ ] **Step 6: Verify in browser**

Remember mode, switch language. Expected: default presets show translated names (Morning meditation / Meditacion matutina); user-created presets keep their literal name; "1 cue" vs "3 cues" correct in both languages.

- [ ] **Step 7: Commit**

```bash
git add src/domain/presets.js src/domain/presets.test.js src/components/PresetLibrary.jsx
git commit -m "feat(i18n): localize PresetLibrary with plurals and shared default-preset-name resolver"
```

---

## Task 13: Session model migration + RecentSessions

**Files:**
- Modify: `src/domain/session.js` (add `sessionDisplay`), `src/domain/session.test.js`
- Modify: `src/App.jsx` (wire `useT`, `persistSession`, `handleRepeatSession`), `src/components/RecentSessions.jsx`

Sessions currently persist preformatted strings (`duration: "20 min"`, `when: "Hoy, 14:32"`, `name`). Migrate to raw data and format at render with the active language. Old sessions fall back to their stored strings. The display/backward-compat logic is a pure, tested function shared by App and RecentSessions.

- [ ] **Step 1: Write the failing test for `sessionDisplay`**

```js
// add to src/domain/session.test.js
import { sessionDisplay } from './session.js';

describe('sessionDisplay', () => {
  const t = (key, vars) => (key === 'sessions.withCue' ? `Sesion con ${vars.name}`
    : key === 'sessions.defaultName' ? 'Sesion completada' : key);
  const fmtDuration = (s) => `${Math.round(s / 60)} min`;
  const fmtDate = () => 'hace 1 hora';

  test('formats a new raw session', () => {
    const item = { cueName: 'Bosque', durationSeconds: 1200, createdAt: '2026-06-14T10:00:00Z' };
    expect(sessionDisplay(item, t, fmtDuration, fmtDate)).toEqual({
      name: 'Sesion con Bosque', duration: '20 min', when: 'hace 1 hora',
    });
  });

  test('falls back to old preformatted fields for legacy sessions', () => {
    const item = { name: 'Sesion vieja', duration: '15 min', when: 'Hoy, 09:00' };
    expect(sessionDisplay(item, t, fmtDuration, fmtDate)).toEqual({
      name: 'Sesion vieja', duration: '15 min', when: 'Hoy, 09:00',
    });
  });

  test('uses default name when neither cueName nor legacy name present', () => {
    expect(sessionDisplay({ durationSeconds: 600 }, t, fmtDuration, fmtDate).name)
      .toBe('Sesion completada');
  });
});
```

- [ ] **Step 2: Run it, verify it fails**

Run: `pnpm test -- --run src/domain/session.test.js`
Expected: FAIL — `sessionDisplay` is not defined.

- [ ] **Step 3: Implement in `src/domain/session.js`**

```js
// Derives display strings for a session card, formatting raw fields with the
// supplied helpers and falling back to legacy preformatted fields for sessions
// saved before the raw model. fmtDuration(seconds) and fmtDate(isoString) are
// passed in so this stays pure (locale lives in the caller).
export function sessionDisplay(item, t, fmtDuration, fmtDate) {
  const name = item.cueName ? t('sessions.withCue', { name: item.cueName })
    : item.name ?? t('sessions.defaultName');
  const duration = item.durationSeconds != null ? fmtDuration(item.durationSeconds)
    : item.duration ?? '';
  const when = item.createdAt ? fmtDate(item.createdAt)
    : item.when ?? '';
  return { name, duration, when };
}
```

- [ ] **Step 4: Run it, verify it passes**

Run: `pnpm test -- --run src/domain/session.test.js`
Expected: PASS.

- [ ] **Step 5: Wire `useT` into App (fixes ordering — App uses `t` below)**

At the top of `App`, add `import { useT } from './i18n/useT.js';` and `const t = useT();`. (App's full notice migration happens in Task 14; this step only makes `t` available for the session changes here.)

- [ ] **Step 6: Persist raw session data**

In `App.jsx`, rewrite `persistSession` and delete the now-unused `nowLabel` helper:

```jsx
function persistSession(seconds) {
  const next = saveSession(window.localStorage, {
    id: crypto.randomUUID(),
    cueName: selectedCue?.name ?? null,   // raw, for the title
    durationSeconds: seconds,             // raw number
    createdAt: new Date().toISOString(),  // raw timestamp
    color: selectedCue?.color ?? '#f6a133',
  });
  setSessions(next);
  setNotice(t('notices.sessionSaved'));
}
```

- [ ] **Step 7: Fix handleRepeatSession to use raw seconds + shared display**

```jsx
function handleRepeatSession(item) {
  const seconds = item.durationSeconds ?? (parseInt(item.duration, 10) || 0) * 60;
  if (seconds > 0) {
    dispatchSession({ type: 'stop' });
    dispatchSession({ type: 'setDuration', durationSeconds: seconds });
  }
  setActiveMode('practice');
  const { name } = sessionDisplay(item, t, (s) => `${Math.round(s / 60)} min`, () => '');
  setNotice(t('notices.repeat', { name }));
}
```

Add `sessionDisplay` to the existing `./domain/session.js` import in App.

- [ ] **Step 8: Migrate RecentSessions to format at render via the shared helper**

Imports:

```jsx
import { useT } from '../i18n/useT.js';
import { useLang } from '../i18n/LanguageContext.jsx';
import { formatDuration, formatRelativeDate } from '../i18n/format.js';
import { sessionDisplay } from '../domain/session.js';
```

Inside the component: `const t = useT(); const { lang } = useLang();`

Per session: `const { name, duration, when } = sessionDisplay(item, t, (s) => formatDuration(s, lang), (d) => formatRelativeDate(d, lang));`

Replace the rendered `{item.name}`, `{item.duration}`, `{item.when}` with `name`, `duration`, `when`. Replace section aria/h2 `Ultimas sesiones` -> `t('sessions.sectionLabel')`/`t('sessions.heading')`, `Ver todo` -> `t('sessions.viewAll')`, empty state -> `t('sessions.empty')`, and the action aria/labels/titles (`Opciones de`, `Repetir`, `Eliminar`, titles) via the `sessions.*Aria`/`repeat`/`delete`/`repeatTitle`/`deleteTitle` keys using `{ name }`.

- [ ] **Step 9: Verify in browser**

Complete a >60s session (or use Stop). Go to Remember.
Expected: new session shows translated name, duration via `formatDuration`, relative date via `formatRelativeDate`; switching language reformats both live. An old session already in storage (preformatted) still renders via its old strings without crashing.

- [ ] **Step 10: Commit**

```bash
git add src/domain/session.js src/domain/session.test.js src/App.jsx src/components/RecentSessions.jsx
git commit -m "feat(i18n): raw session model with shared locale-aware display helper, backward compatible"
```

---

## Task 14: App.jsx remaining strings

**Files:**
- Modify: `src/App.jsx`

- [ ] **Step 1: Replace notices + inline strings**

`useT`/`t` are already wired into `App` in Task 13. This task replaces the remaining literals.

- Replace every `setNotice('...')` literal with its key:

| Literal | Key |
|---|---|
| `Sesion guardada en Recordar` | `notices.sessionSaved` |
| `Preset guardado` | `notices.presetSaved` |
| `Cue añadida` | `notices.cueAdded` |
| `Cue duplicada` | `notices.cueDuplicated` |
| `Cue eliminada` | `notices.cueDeleted` |
| `Cambio deshecho` | `notices.changeUndone` |
| `Preset cargado: ${preset.name}` | `t('notices.presetLoaded', { name: presetLabelForNotice })` |
| `Preset eliminado` | `notices.presetDeleted` |
| `Sesion eliminada` | `notices.sessionDeleted` |
| `Duracion actualizada` | `notices.durationUpdated` |
| `Datos copiados al portapapeles` | `notices.dataCopied` |
| `Datos importados` | `notices.dataImported` |
| `JSON no valido` | `notices.invalidJson` |

For `presetLoaded`, reuse the shared `resolvePresetName(preset, t)` from `domain/presets.js` (add it to App's existing presets import): `setNotice(t('notices.presetLoaded', { name: resolvePresetName(preset, t) }))`.
- Replace the design preset-name bar literals: placeholder `Nombre del preset` -> `t('presetBar.placeholder')`, aria -> `t('presetBar.nameAria')`, button `Guardar preset` -> `t('presetBar.save')`, title -> `t('presetBar.saveTitle')`.
- Default preset/session fallback names used when saving (`'Preset Sadhana'`) -> keep literal or add a key `presetBar.defaultName` (add to es/en if used).

- [ ] **Step 2: Verify in browser**

Trigger several notices (add cue, save preset, change duration) in en and es. Expected: notices appear in the active language. Preset-name bar localized.

- [ ] **Step 3: Run full test suite**

Run: `pnpm test -- --run`
Expected: all green (existing + new i18n tests).

- [ ] **Step 4: Commit**

```bash
git add src/App.jsx
git commit -m "feat(i18n): localize App notices and design preset-name bar"
```

---

## Task 15: Pali easter egg + final pass

**Files:**
- Modify: `src/i18n/locales/pi.js`, `index.html`

- [ ] **Step 1: Fill pi.js with natural Pali (IAST) terms**

Only keys that have a real, recognizable Pali term. Everything else stays absent (falls back to es). Suggested seed (extend to taste):

```js
// src/i18n/locales/pi.js
export default {
  modes: { practice: 'Bhavana', design: 'Sankhara', remember: 'Sati' },
  app: { title: 'Sadhana' },
  timer: { panelLabel: 'Kala' },
  inspector: { instruction: 'Anusasana' },
};
```

Keep it tasteful and partial — the point is the fallback gracefully covers the rest.

- [ ] **Step 2: Confirm index.html base lang**

`index.html` keeps `<html lang="es">` as the pre-hydration default; the provider overrides it on mount per the stored/navigator language. No change needed unless it drifted — verify it still reads `lang="es"`.

- [ ] **Step 3: Verify in browser**

Select Pali in the selector. Expected: the few translated terms (modes tabs) show Pali; everything else shows Spanish; `document.documentElement.lang` is `pi`; no missing-key warnings for the absent keys (they fall back silently — only fully-absent-everywhere keys warn).

- [ ] **Step 4: Full regression**

Run: `pnpm test -- --run`
Expected: all green.

Manual: cycle es -> en -> pi, visit Practice/Design/Remember, open all three panels. Expected: no raw keys visible, no console errors, layout stable.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/locales/pi.js
git commit -m "feat(i18n): pali easter-egg partial dictionary"
```

---

## Self-Review (completed during planning)

- **Spec coverage:** casero resolver (T1), Intl format (T2), registry+navigator+dir (T3), indexed dicts + integrity test direction es-canonical (T4), provider+lang/dir/title+persistence (T5), list selector in Settings (T6), all components incl. aria/title/placeholder (T6-T14), translatable sound groups (T11), translatable default presets by id (T12), raw session model + locale dates (T13), plurals via Intl.PluralRules (T1/T12), missing-key behavior (T1), pali partial (T15). RTL/fonts are registered (dir in T3/T5) but `ar` CSS/fonts remain out of scope per spec.
- **Placeholders:** none — every step has concrete code or an explicit literal→key mapping.
- **Type consistency:** `translate(locales, lang, baseLang, key, vars)`, `t(key, vars)`, `useT()`, `useLang()`, `formatDuration(seconds, lang)`, `formatRelativeDate(date, lang, now)`, `resolvePresetName(preset, t)`, `sessionDisplay(item, t, fmtDuration, fmtDate)`, `LANGUAGES`/`BASE_LANG`/`pickInitialLang`/`resolveDir` used consistently across tasks.
- **Shared pure helpers (tested):** `resolvePresetName` (domain/presets.js, T12) used by PresetLibrary + App; `sessionDisplay` (domain/session.js, T13) used by RecentSessions + App. No duplication.
- **Ordering:** Task 13 wires `useT` into App (Step 5) before any App code uses `t`, so tasks are independently executable in order. Task 14 finalizes the remaining App literals.
- **Resolver robustness:** plural key without `n` falls back to `other` (no `RangeError`); missing-everywhere key returns the raw key (dev-only warn, not asserted in tests).
