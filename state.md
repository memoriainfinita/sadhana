---
created: 2026-05-09
last_updated: 2026-06-14
---

# Sadhana State

## System

- Workspace: `D:\DOCS\CODELAB\1_in-progress\sadhana-v0` (movido desde OneDrive 2026-06-05)
- Project: Sadhana, app audiovisual/musical para sesiones guiadas con audio, cues y memoria de practica.
- Stack: React + Vite, pnpm, Git en rama `master`

## Services

- Dev server: `pnpm dev` en `http://localhost:5173/` (si el puerto esta libre; usar `--port 5200` si hay conflicto).

## Preferences

- Conversacion en espanol.
- Comandos, nombres tecnicos y commits en ingles.
- Cierre de sesion mediante `state.md`, no mediante archivos ad hoc de cierre.

## Patterns

- [app-modes] La nueva Sadhana se organiza en tres modos obligatorios, no alternativos: `Practicar`, `Disenar`, `Recordar`. Confirmed 2026-05.
- [visual-target] El concepto aprobado usa una interfaz oscura, sobria, con acento ambar y tres zonas principales: temporizador, timeline/cues e inspector. Confirmed 2026-05.

## History

- 2026-05-09/10: Sesiones iniciales — concepto aprobado, app React/Vite creada, modos separados, CRUD de cues, presets, sesiones, commit inicial `6e44540`.
- 2026-06-05: Proyecto movido de OneDrive a `D:\DOCS\CODELAB\1_in-progress\sadhana-v0`, pnpm instalado con approve-builds para esbuild.
- 2026-06-05: Ronda de UX en Practicar: botones ±15 arreglados, barra de progreso, modo zen (fullscreen), cues ocultables, trigger de audio al click (luego revertido a lista pasiva), notificacion de cue activa bajo el timer con fade suave.
- 2026-06-05: Ronda de UX en Diseñar: markers arrastrables para mover tiempo de cues, playhead arrastrable para seek, barra de playback (sin barra de progreso), botones Añadir/Deshacer/Buscar movidos al header del timeline, zoom eliminado, nombre de preset editable + guardar en barra superior.
- 2026-06-05: Audio: volumen maestro y mute ahora afectan audio en reproduccion (applyMasterVolume), fades in/out implementados con rAF, color de acento seleccionable desde panel Tema.
- 2026-06-05: Flujos: cargar preset va a Practicar, repetir sesion restaura duracion, sesiones se guardan tambien al detener manualmente (>60s), tooltips en todos los botones.
- 2026-06-05: Layout Practicar: cues integradas dentro del TimerPanel debajo del boton Detener, lista compacta pasiva, panel centrado columna unica.
- 2026-06-06: Batida de codigo muerto: ModePanel.jsx eliminado (huerfano), prop onSavePreset eliminada de CueInspector, bug en OLD_SAMPLE_NAMES corregido ('Nueva cue'/'Nueva cue copia' ya no filtran presets legítimos).
- 2026-06-06: Tests: arreglados 2 tests incorrectos (scheduler arg fantasma, cobertura de sonidos), añadidos 4 tests nuevos (clampCueTime, savePreset, saveSession, regresion cleanStoredExamples). Total: 24 tests verdes.
- 2026-06-06: Registro completo de audios: plugin Vite `virtual:audio-manifest` autodescubre `public/audio/**/*.mp3` al arrancar. SOUND_OPTIONS y SOUND_OPTION_GROUPS generados dinamicamente desde `src/domain/sounds.js`. CueInspector usa `<optgroup>` por categoria. Renombrado `bells/3 chime-meditation.mp3` → `bells/chime-meditation.mp3`. 51 archivos registrados.
- 2026-06-06: Disenado y planificado campo `instruction` en cues. Spec: `docs/superpowers/specs/2026-06-06-instruction-field-design.md`. Plan: `docs/superpowers/plans/2026-06-06-instruction-field.md`.
- 2026-06-07: Campo `instruction` implementado — 7 tareas via subagent-driven-development, 6 commits (b0b3a98..808d308), 28 tests verdes. Modelo extendido (instruction/instructionDuration), storage key, App state + scheduler, TimerPanel con dos zonas de display y fades asimetricos, GlobalPanel con toggle "Mostrar nombre del sonido", CueInspector con campos Instruccion + Visibilidad (s).
- 2026-06-08: Atajos de teclado — Space (play/pause/resume), ←→ nudge ±15s (solo Practicar), F (zen), Escape (salir zen), 1/2/3 (modos). Un useEffect con ref para evitar closures rancias. Commit 8342788.
- 2026-06-08: Fix audio/preview — sesion para automaticamente al cambiar de modo (useEffect en activeMode). Seek hacia atras resetea scheduler (borra cues con time > newElapsed de playedCueIds). Commit 65509dc.
- 2026-06-08: 5 presets por defecto — Pranayama 4-7-8 (15 min), Meditacion matutina (20 min), Relajacion profunda (30 min), Caminata consciente (10 min), Yoga Nidra (30 min). Semillados via seedDefaultPresets en primer arranque. src/domain/presets.js. Commits e9ed3f2, c97497f.
- 2026-06-08: Tests ampliados — seedDefaultPresets (2), backward seek scheduler (1), integridad DEFAULT_PRESETS (5). Total: 36 tests verdes en 7 archivos. Commit 5b79296.
- 2026-06-07: Audio fixes — cue overlap en backward seek (stopCue antes de playCue + duration timeout con stopInstance), fade/master-volume conflict (AudioRegistry guarda _masterVolumeScale/_muted, entries con cueVolumeScale, _rampVolume lee estado live). 5 commits (df742e1..73c8f7d), 43 tests verdes. Specs: docs/superpowers/specs/2026-06-07-audio-fixes-design.md.
- 2026-06-13: Auditoria de codigo y limpieza. (1) cleanStoredExamples ya no filtra por nombre en cada arranque: nueva cleanStoredExamplesOnce con flag examplesCleaned, limpia una vez para usuarios viejos y nunca mas (eliminado riesgo de borrar presets/sesiones del usuario por coincidencia de nombre). (2) Codigo muerto eliminado: handleTriggerCue (feature trigger-al-click revertida) y public/audio/audio-list.js (huerfano, reemplazado por virtual:audio-manifest, via git rm). (3) Bug: slider Tiempo de CueInspector usaba max=1440 fijo, ahora usa durationSeconds (permite colocar cues en sesiones >24 min). (4) Guardado de sesion deduplicado en helper persistSession. (5) Datos demo ficticios reemplazados por estados vacios reales en PresetLibrary y RecentSessions. (6) DEFAULT_CUES ahora llevan instruction/instructionDuration igual que createCue.
- 2026-06-13: Identidad visual. Favicon SVG (`public/favicon.svg`, "S" ambar #f6a133 sobre cuadrado oscuro redondeado) + `<link rel="icon">` en index.html (resuelve el 404 de favicon en Chrome). El brand-mark del header (antes `NamasteMark`, unas manos en namaste que no se leian bien) reemplazado por la misma "S" ambar, renombrado a `BrandMark`, manteniendo el contenedor circular con borde ambar.
- 2026-06-13: Instruccion de la cue activa en la PlaybackBar (modo Diseñar). Funcion pura `getActiveInstruction(cues, elapsedSeconds)` en domain/cues.js: devuelve la instruccion de la cue cuyo rango `[time, time + instructionDuration)` cubre el elapsed (la de mayor `time` si solapan, `''` si ninguna). Es derivada del tiempo actual, no de timeouts, asi que responde al seek y a pausa. App la pasa como prop `instruction` a PlaybackBar, que la renderiza junto al tiempo. Orden en la barra: controles, tiempo, instruccion (el tiempo queda fijo y no salta al aparecer/desaparecer la instruccion). 5 tests nuevos, 66 verdes. Verificado a mano.
- 2026-06-13: Verificado que el dev server con Bash run_in_background persiste en Windows (Vite ready, puerto escuchando, gestionable con TaskStop). La regla antigua de memoria (usar Start-Process) ya no aplica; memoria feedback-devserver-windows actualizada.
- 2026-06-13: Handles de clips en el timeline (modo Diseñar). domain/clip.js con `pxDeltaToSeconds`, `clampDuration`, `clampFadeIn`, `clampFadeOut` (12 tests). TrackClip.jsx: rampas de fade siempre visibles + 3 handles que aparecen en hover (resize en borde derecho → `duration`, fadeIn esquina sup-izq, fadeOut esquina sup-der), pointer capture, valores locales durante drag. TrackRow en Timeline.jsx: `track-row` pasa de `<button>` a `<div role="button">` con ref per-row al `.track-line` para el calculo de coordenadas. handleResizeCue/handleFadeCue en App (resize re-clampea fades para que no superen la duracion). Eliminado `min-width` del clip: el ancho refleja la duracion real. Spec corregido antes de implementar (3 fallos: contenedor de coordenadas `.tracks` vs `.track-line`, min-width rompia el mapeo, jerarquia DOM de 3 niveles). 6 commits. 61 tests verdes. Plan: docs/superpowers/plans/2026-06-13-timeline-clip-handles.md. Verificado a mano en el navegador.
- 2026-06-14: Diseño y planificación de i18n (sin implementar todavía). Brainstorming completo: enfoque casero (LanguageContext + useT + diccionarios indexados, no react-i18next), multi-idioma desde el inicio con selector tipo lista. Alcance fase B: infra + es/en completos + pali parcial (easter egg, romanizado IAST). 16 idiomas objetivo registrados (incluido arabe RTL, contemplado pero no implementado). Decisiones cerradas tras dos rondas de busqueda de huecos: datos del usuario no se traducen pero presets por defecto si (via t('presets.<id>') con fallback), categorias de sonido si y nombres individuales no, fechas/duraciones/plurales con Intl (RelativeTimeFormat/PluralRules), modelo de sesion migra a datos crudos con retrocompat, test de integridad con es canonico (en debe cubrirlo). Spec: docs/superpowers/specs/2026-06-14-i18n-design.md (commits dcbe19e, 61d2e18). Plan de implementacion de 15 tareas: docs/superpowers/plans/2026-06-14-i18n.md (commits 4855a84, 68e4392). Sin jsdom: TDD solo en dominio puro (translate, format, resolvePresetName, sessionDisplay), UI verificada a mano. Pendiente: ejecutar el plan. Tambien añadido TODO de auditoria WCAG completa como sesion separada.
- 2026-06-13: Duracion de presets como fuente de verdad. Los 5 presets por defecto llevan durationSeconds; savePreset guarda session.durationSeconds; handleLoadPreset aplica la duracion al cargar (antes se quedaba siempre en 24 min). Funcion presetDurationSeconds(preset) extraida a domain/presets.js (usa durationSeconds o deriva del fin de la ultima cue redondeado al minuto) y usada por App y por la tarjeta de PresetLibrary (muestra "N cues · M min"). Renombrado "Yoga Nidra 30 min" -> "Yoga Nidra" (el tiempo en el nombre era decorativo y no cuadraba). 49 tests verdes.

- 2026-06-14: Auditoria WCAG — diseño y plan completos, SIN implementar todavia. Auditados los 11 componentes + styles.css. 13 hallazgos iniciales en tres bloques: A (teclado: no existe `:focus-visible` en todo el CSS, playhead/handles/markers del timeline no operables por teclado), B (estado/semantica: faltan aria-current/aria-pressed/aria-expanded/aria-live), C (movimiento/contraste/foco: sin prefers-reduced-motion, `--faint` falla AA con 3.57, foco de paneles sin gestionar, sin h1). Contrastes medidos: el acento pasa AA con holgura (8-9:1); solo `--faint` falla (se sube a #8f8b82, 4.97 sobre panel). Dos rondas de busqueda de huecos: sobre la spec +2 hallazgos (A15 el guard global de teclado intercepta Space/flechas cuando un widget del timeline tiene foco; B14 estado de seleccion de markers/track-rows solo visual); sobre el plan +4 correcciones (P1 los markers no tienen onClick, no se seleccionarian por teclado; P2 dos `outline:none` anulan el focus-visible por especificidad; P3 Escape bloqueado por el guard de inputs; P4 aria-label redundante en el brand). Decisiones: teclado del timeline hibrido (markers+playhead operables por flechas/Home/End con paso 5s, handles delegados al CueInspector y marcados aria-hidden); paneles como disclosure no modal (foco entra al abrir, Escape/X/re-pulsar cierran y devuelven foco); A15 resuelto con stopPropagation en los widgets (no ensanchar el guard, para no romper los atajos de modo). Diferido a TODO propio: WCAG 1.4.11 non-text contrast (bordes de controles ~1.3:1). Spec: docs/superpowers/specs/2026-06-14-a11y-audit-design.md (commits fc419bf, 0228b66). Plan de 10 tareas: docs/superpowers/plans/2026-06-14-a11y-audit.md (commits 0985105, 0ef4a2f). Sin jsdom: TDD solo en stepFromKey, resto verificado a mano. Pendiente: ejecutar el plan inline en la proxima sesion.
- 2026-06-14: Auditoria WCAG implementada (fase A/B/C de la spec). Plan de 10 tareas ejecutado inline en master, 11 commits (388... -> ultimo). T1 funcion pura `stepFromKey` (domain/keyboard.js) con TDD: ArrowRight/Up +step, ArrowLeft/Down -step, Home/End a min/max, clamp, null para teclas no manejadas (9 tests). T2 playhead operable por teclado (role=slider, tabIndex, aria-value*, flechas/Home/End con paso 5s via stepFromKey + stopPropagation para no disparar el atajo global; clave i18n timeline.playheadAria en es/en). T3 markers por teclado (flechas mueven cue.time, Space/Enter seleccionan via onSelectCue explicito por no haber onClick, stopPropagation) + aria-pressed en markers y track-rows. T4 los 3 handles de TrackClip con aria-hidden (redundantes con CueInspector). T5 aria-current=page en el modo activo (ModeTabs). T6 aria-expanded en los 3 botones de panel del topbar, brand `<span>`->`<h1 className="brand-title">` (quitado aria-label redundante del .brand), cada boton pasa e.currentTarget como trigger. T7 GlobalPanel: foco entra al panel al abrir/cambiar (useEffect+ref, primer focusable o el aside con tabIndex=-1) + aria-pressed en swatches de acento. T8 App: lastPanelTrigger ref + handlePanelChange captura el disparador, useEffect restaura foco al cerrar (activePanel===null), Escape cierra panel antes del guard de inputs (movido el destructuring arriba, eliminado el viejo case Escape del switch), shortcutsRef extendido con activePanel/setActivePanel, prop announce a los dos TimerPanel (zen: announce; practice: announce={!zenMode}, una sola live region). T9 TimerPanel: prop announce=false, aria-live=polite condicional en playing-instruction-label (el nombre del sonido NO, decision GAP-7), aria-expanded en el toggle de cues. T10 CSS: --faint #77736b->#8f8b82 (AA 4.97 sobre panel), quitados los dos outline:none (preset-name-input y cue-inspector input/select/textarea) que anulaban el anillo por especificidad, anillo global :focus-visible 2px var(--accent) con :where(...) + anillo interno (outline-offset -2px) para markers/playhead/track-row en panel overflow:hidden, .brand-title hereda tamano/peso, bloque prefers-reduced-motion (transition/animation-duration 0.01ms). 97 tests verdes (88 baseline + 9 de keyboard.test.js). Build de produccion limpio. Verificacion automatizada completa; verificacion visual en navegador PENDIENTE (checklist manual de la spec: foco visible sin recorte, flechas en timeline, Space selecciona, atajos de modo intactos con foco en boton, foco de paneles, anuncio unico de instruccion, reduced-motion). Spec: docs/superpowers/specs/2026-06-14-a11y-audit-design.md. Plan: docs/superpowers/plans/2026-06-14-a11y-audit.md.
- 2026-06-14: i18n implementado (sistema casero, fase B). Plan de 15 tareas ejecutado inline en master, 15 commits (a99f65e..5ff19cb). TDD en dominio puro: translate (resolver con fallback es, interpolacion {var}, plurales via Intl.PluralRules), format (formatDuration/formatRelativeDate via Intl), index (registro de 16 idiomas + pickInitialLang + resolveDir RTL para ar), integridad de locales (en cubre es), resolvePresetName (presets.js), sessionDisplay (session.js). LanguageContext + useT + selector tipo lista en Configuracion + persistencia en localStorage (STORAGE_KEYS.lang) + gestion de html lang/dir y document.title. Diccionarios es (canonico) + en (completo) + pi (parcial, easter egg: modos, timer, instruccion). Los 10 componentes + App migrados (texto, aria-label, title, placeholder). Grupos de sonido traducibles por carpeta (sounds.js expone folder), presets por defecto traducidos por id, modelo de sesion migrado a datos crudos (cueName/durationSeconds/createdAt) retrocompatible con sesiones viejas (duration/when string). 88 tests verdes (66 baseline + 22 nuevos). Build de produccion limpio (1607 modulos). Verificacion automatizada completa; verificacion visual en navegador confirmada por el usuario (todo funciona). Spec: docs/superpowers/specs/2026-06-14-i18n-design.md. Plan: docs/superpowers/plans/2026-06-14-i18n.md.
- 2026-06-26: Verificacion visual WCAG en navegador completada (checklist manual de la spec). 7 items verdes: foco visible sin recorte (-15/marker/engranaje), flechas en timeline (marker 4:00->5:00 paso 5s), Space selecciona cue sin disparar play global, atajos de modo intactos con foco en widget ("3"->Remember), foco de paneles (entra al abrir, Escape cierra y restaura foco al disparador), region aria-live unica (.playing-instruction-label polite, nombre de sonido no-live), regla CSS reduced-motion presente. Confirmado el fix A15 (stopPropagation no filtra a atajos globales). Sin cambios de codigo, solo verificacion.
- 2026-06-26: WCAG 1.4.11 Non-text Contrast resuelto. Medido: --line (#2a302f, 1.33:1) y --line-strong (#38403e, 1.68:1) fallaban contra el fondo efectivo de panel (rgb 19,24,23); el relleno de input (#0f1312) es casi identico al panel (~1:1), asi que el borde es lo unico que identifica el control. Token nuevo --control-border #626a67 (3.23:1 sobre panel, 3.37:1 sobre relleno de input) aplicado solo a controles interactivos (inputs y botones con borde: preset-name, cue-inspector input/select/textarea, select-row, timeline-search, global-panel input/textarea, duration, accent-color, round-button +-15, stop, Add, timeline-toolbar, mode-note/global-panel/preset-card button, section/recent-header button, recent-card button). Divisores, bordes de panel, eje del timeline y cards de contenido siguen con --line/--line-strong (1.4.11 no los exige; se preserva la estetica sobria). Solo CSS, 15 selectores + token. Verificado por DOM (textarea 3.23:1, select nativo 3.95:1) y a ojo en Practice/Design/Configuracion. 97 tests verdes, build limpio (1608 modulos).

## TODO

- [x] Campo `instruction` en cues — implementado 2026-06-07.
- [x] Audio scheduling — overlap en seek y fade/volume conflict resueltos 2026-06-07. Precision de timing (250ms) aceptable, no requiere cambio.
- [x] i18n: implementado y verificado 2026-06-14 (fase B: infra + es/en completos + pali parcial).
- [x] Auditoria de accesibilidad WCAG (separada de i18n): plan de 10 tareas ejecutado e implementado 2026-06-14, 11 commits, 97 tests verdes, build limpio. Verificacion visual en navegador completada 2026-06-26 (7 items del checklist en verde). Plan: docs/superpowers/plans/2026-06-14-a11y-audit.md.
- [x] WCAG 1.4.11 Non-text Contrast (diferido de la auditoria WCAG): bordes de controles a >=3:1 via token --control-border #626a67. Implementado y verificado 2026-06-26.
- [x] Accesos de teclado — implementado 2026-06-08.
- [x] Crear presets basicos de ejemplo — 5 presets implementados 2026-06-08.
- [x] Timeline track clips redimensionables: arrastrar el borde derecho del clip para cambiar `duration` de la cue, al estilo DAW. Implementado 2026-06-13.
- [x] Handles de fade en los clips del timeline: esquina superior izquierda para fade in, esquina superior derecha para fade out — arrastrar horizontalmente cambia `fadeIn`/`fadeOut`, con indicador visual de la rampa, al estilo Reaper. Implementado 2026-06-13.
- [x] Diseñar: en la barra de controles de reproduccion encima del timeline, mostrar la instruccion de la cue activa junto al tiempo. Implementado 2026-06-13.
- [x] Batida de codigo muerto y features a medio implementar.
- [x] Registrar todos los audios en SOUND_OPTIONS.
- [x] Persistir presets y sesiones de forma exportable/importable.
- [x] Decidir si `sadhana-next` se convierte en repo Git propio o si se inicializa Git en la raiz del proyecto.
